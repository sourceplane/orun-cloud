# Task 0138 — Milestone IG0: integrations foundation (dormant)

# Agent

Implementer

# Current Repo Context

The saas-integrations epic (IG0–IG7) merged as #302. PX3 (#303) shipped.
IG0 is the human-independent contract-and-schema slice; IG1+ live paths are
gated on the per-environment GitHub App registration (epic D1).

# Objective

Land the integrations bounded context with zero live behavior: component
contract, public contracts module, `180_integrations_foundation` migration +
repo layer, and a dormant `apps/integrations-worker` skeleton wired into Orun
discovery.

# Architect Brief

- Bar: indistinguishable from the shipped bounded contexts (webhooks/billing
  discipline applied to a new schema).
- Free: repo-layer method set beyond the design's table list, test shape.
- Must not: expose any public route beyond `/health`; store any provider
  credential as a row; diverge from design.md §3 table shapes.
- Failure modes: a reachable non-health route; manifest checksum mismatch;
  a contracts shape that leaks tokens/raw payloads.

# Milestone Boundary

`specs/components/17-integrations.md`, `packages/contracts`, `packages/db`,
`apps/integrations-worker`, `tests/db`, `tests/integrations-worker`. No edge,
console, SDK, or CLI changes (those are IG1+).

# Read First

- `specs/epics/saas-integrations/design.md` (§3 data model, §2 seam)
- `specs/epics/saas-integrations/implementation-plan.md` (IG0 "done when")
- `apps/webhooks-worker` (worker anatomy), `packages/db/src/billing` (repo layer)

# Required Outcomes

1. `specs/components/17-integrations.md` component contract.
2. `packages/contracts/src/integrations.ts` — PublicConnection,
   PublicRepoLink, PublicInboundDelivery, connect/link/token shapes,
   versioned `scm.*` projections; export wiring.
3. `packages/db`: migration `180_integrations_foundation` (five tables per
   design §3, manifest entry + checksum), `src/integrations` repo layer,
   `BOUNDED_CONTEXTS` updated.
4. `apps/integrations-worker` skeleton (router, health, env, ids, cron stub,
   component.yaml, wrangler.jsonc dev/stage/prod) + test package.

# Non-Goals

Connect flow, ingress routes, provider adapter, console surface, token
minting — all IG1+.

# Acceptance Criteria

Typecheck/lint/test green across the workspace; migration applies on stage
via db-migrate on merge; deployed dormant worker answers `/health`; no other
route reachable.

# Verification

Suite runs + post-merge stage health check.

# PR Creation Requirement

PR with green CI before merge.

# When Done Report

`ai/reports/task-0138-implementer.md`.
