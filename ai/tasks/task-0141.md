# Task 0141 — Milestone IG3: repo links + branch→environment mapping

# Agent

Implementer

# Objective

Projects link provider repositories with branch → environment maps; linked
repos emit project-scoped `scm.*` events with the environment resolved; a
repo picker rides the platform's cached installation token. Live browsing
parks on D1; everything else is fixture/unit verified.

# Required Outcomes

1. Repo browsing: `GET .../integrations/{id}/repositories` via App JWT →
   installation token (AES-GCM cached, expiry margin, re-mint).
2. `repo_links` CRUD under `/v1/organizations/{orgId}/projects/{projectId}/
   repo-links` — policy `project.repo_link.write` (org owner/admin +
   project_admin), entitlement `limit.repo_links` (412 limit_reached with
   usage), branch maps validated against live environments via a new
   projects-worker internal seam.
3. Drain enrichment: linked repos emit per-project with projectId +
   environment (push → branch, PR → target branch); unlinked stay org-scoped.
4. `scm.repo.linked` / `scm.repo.unlinked` events.
5. Console project **Git** tab: repo picker (search), link with suggested
   branch map, mapping badges, unlink confirm, designed empty states.
6. SDK methods; api-edge forwarding (repo-links paths + PATCH).

# Acceptance Criteria

Workspace suites green; cross-project/org tenancy 404s proven; limit gate
proven with usage details; map validation rejects unknown environments.

# When Done Report

`ai/reports/task-0141-implementer.md`.
