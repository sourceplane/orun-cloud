# saas-dispatch — Implementation Status (as-built)

The as-built record, kept distinct from the design/plan docs. Nothing is built
yet — this epic is **proposed**. Rows flip to 🏗️/✅ as DX milestones land.

| Milestone | Status | As-built |
|-----------|--------|----------|
| DX0 — The Situation read-model | ✅ Shipped | `packages/contracts/src/dispatch.ts` — the `Situation` wire shape (plane-tagged `SituationReadyItem`/`SituationSessionItem`/`SituationAttentionItem`, `SituationBudget`, per-section `unavailable` degradation flags, `situation_unavailable`), registered as `@saas/contracts/dispatch`. `apps/api-edge/src/dispatch-facade.ts` — `GET /v1/organizations/{orgId}/dispatch/situation`: resolves the actor once, stamps `x-actor-*`, fans out in ONE parallel round to state-worker `GET /work` (the fold) + agents-worker `sessions`/`attention`/`budgets`, and composes: ready = fold-rung `ready` ∧ unassigned with the fold's evidence verbatim; in-flight = live infra states; waiting-on-me = the AF5 attention fold passed through with provenance (plane-tagged `session`/`governance`); budget = workspace-grain ceiling + live-session token sum (labeled a live view, never the billing meter); **cursor = the work fold's real sequence watermark `w<coordSeq>.<obsSeq>`** (a design upgrade over the spec's abstract cursor — adopted for DX1's incremental refresh). Sections cap at 50 and degrade independently; 503 only when every source fails; the viewer's bearer never travels downstream (actor stamping only). SDK: `dispatch.situation(orgId)` (`DispatchClient`). Tests: 11 facade tests (`tests/api-edge/src/dispatch-facade.test.ts` — the per-viewer stamping invariant, degradation matrix, 401-before-fanout) + 1 SDK URL-shape test. Verified: contracts+sdk build, api-edge typecheck + lint, api-edge suite (3 pre-existing unrelated failures on main: auth/project/org facades — confirmed by stash-run), sdk 213 tests green. |
| DX1 — DispatchIndex + live push | ✅ Shipped (reporter-doorbell v1; ES-lane feeder is a later third ringer) | **Design amendment (recorded per §2.2's own generalization):** the doorbell is not wired to the ES lane in v1 — chat-worker is unprivileged and gets no lane binding. Instead the object is a **cursor rendezvous**: (1) any HEAD that refolds and sees the `w<coord>.<obs>` watermark advance reports it (`situation:report`), and the object fans `situation:invalidate` to every OTHER head — one head's 30s backstop poll becomes everyone's push; (2) chat-worker **rings** after every successful Workspace Agent turn (`ring("inFlight")` — the surface's own spawns/steers echo instantly); (3) the ES-lane consumer can ring later without touching the object. As-built: `apps/chat-worker/src/dispatch-core.ts` — the pure `DispatchIndexCore` (storage-injectable, ChatThread discipline): snapshot-first shell (`situation:shell` = cursor + counts ONLY — no authorized content, unit-asserted), advancing-report persist + fan-out-except-reporter, stale/equal-report no-ops (idempotent convergence), section-tagged `ring`, ping/pong, junk-frame tolerance. `dispatch-index.ts` — the `DispatchIndex extends Agent` SQLite DO shell (`wsdx:<orgId>`, hibernatable WS) + typed RPC `ring`/`shell`. Router: `GET /v1/organizations/{orgId}/dispatch/index` — WS upgrade forwarded to the DO, plain GET serves the shell; `organization.agent.chat` grant; ring-after-turn wired. `wrangler.template.jsonc`: `DISPATCH_INDEX` binding (top-level + 3 envs) + migration `v3 new_sqlite_classes`. api-edge: the index route joins the dispatch facade — WS pass-through to CHAT_WORKER with actor stamped, `access_token` query bearer accepted and STRIPPED (the AL attach rule). Tests: 12 core/router tests (`tests/chat-worker/src/dispatch-index.test.ts`) + 2 facade tests. Verified: chat-worker 58 + api-edge dispatch 13 green; typecheck + lint both workers; `wrangler deploy --dry-run` (fixture-rendered) resolves bindings + migration. |
| DX2 — The Dispatch surface | ✅ Shipped (v1 — left pane is the thread LIST; the single-thread command-line fusion + Cmd-K ride DX3/DX5) | `/orgs/:slug/dispatch` — the two-pane surface: left the Workspace Agent threads (`WorkspaceChatList`, create/open in place), right the live **Situation rail**. `lib/dispatch/model.ts` — pure presentation model: `situationCounts` (the DX1 report payload — aggregate numerals only, unit-asserted), `budgetView` (soft-mark tones, clamped), `readyCard`/`sessionCard`/`attentionCard` as **structurally distinct shapes** (the D5 guard test pins that a work card has no session-state field and a session card no rung/lifecycle/evidence field — a plane merge fails the type AND the test), org-scoped href helpers, `unavailableSections` for the honest degradation chip. `lib/dispatch/use-situation.ts` — the fold via the DX0 facade + the DX1 socket: reports `{cursor, counts}` after every successful fold, refolds on `situation:invalidate` (250ms debounce), reconnects with backoff, 30s **backstop** interval (explicitly not the hot path), transport chip (`live`/`polling`). `components/dispatch/situation-rail.tsx` — Ready (Dispatch button → the ONE AG9 door via new SDK `agents.dispatchTask`; refusal reasons render verbatim; parked-on-gate surfaced), In-flight (state pill via `sessionTone`, child indent, taskKey as a LINK), Waiting-on-you (verdict cards say "answered on the session page" — lock 5), Budget (ceiling bar + live-wire chip). SDK: `dispatchTask(orgId, {taskKey, specKey?})`. Tests: 12 model tests (`dispatch-model.test.ts`) + 2 SDK. Verified: console 736 tests + typecheck + lint + full OpenNext build; sdk 214. |
| DX3 — The front door | ✅ Shipped | **Design amendment (supersedes DX-Q2's `feature.dispatch_home` server flag):** the landing decision must be synchronous — the front door cannot block on a config read without violating the epic's own snapshot-first budget, and the console has no server-flag evaluation plumbing. So the landing is a per-browser, per-workspace **preference with dispatch as the default** (`lib/dispatch/landing.ts`, key `sp.landing.<slug>`, junk/denied-storage tolerant); a workspace-level ops kill-switch can ride the config surface later without changing the seam. As-built: the **org root renders Dispatch directly** (no redirect hop — `DispatchSurface` extracted to `components/dispatch/dispatch-surface.tsx`), or the Overview when the preference says so; `/orgs/:slug/overview` is the demoted, content-unchanged Overview with a **LandingToggle** ("Make Dispatch/Overview my landing" — the swap is a default, never a lock-in); `/orgs/:slug/dispatch` stays as a stable alias that redirects home. `nav-items.ts`: **Dispatch** is the home row at the org root (exact-match highlighting preserved), **Overview** beside it at `/overview`. Tests: nav expectations updated + 3 landing tests (default-dispatch, per-slug round-trip, junk/denial tolerance). Verified: console 739 tests + typecheck + lint + full OpenNext build. |
| DX4 — Proactive dispatch | ✅ Shipped (pull-rendered v1; model-driven scheduled brief + AF routine target stay deferred with AN6's agent-principal tail) | **Design note (recorded):** the risks table's own posture is implemented literally — "the brief is a SURFACE (pull-rendered from the Situation), not a second doorbell." A model-composed *scheduled* turn needs a credential the unprivileged chat plane does not hold at schedule time (lock 4/6 — the same reason AN6 deferred its proactive half), so it waits on the agent-principal credential; AL8 remains the only push channel. As-built: `lib/dispatch/brief.ts` — pure `composeBrief` (ready/in-flight/waiting digests with plurals; the budget line only past the soft mark; **all-quiet → null, an empty brief never renders**), `pendingBadgeCount` (viewer-agnostic shell counts only), visit + mute preferences (per-workspace, denial-tolerant). `components/dispatch/brief-card.tsx` — the "Standing brief" card atop the Dispatch surface with Mute/Dismiss (dismiss stamps the visit). `components/dispatch/pending-badge.tsx` + sidebar wiring — the Dispatch home row wears the needs-you numeral from the DX1 shell (one cheap GET, zero authorized content in chrome; zero renders nothing). SDK: `dispatch.shell(orgId)`. Tests: 5 brief/badge tests. Verified: console 747 tests + typecheck + lint + full OpenNext build; sdk 214. |
| DX5 — Responsiveness + trust hardening | 🗓️ Not started | — |
| DX6 — Provider & model settings | ✅ Shipped (chat ModelClient parity parked on DX-Q6) | The provider widening shipped on the identical AG12 path, verified locally (builds, typecheck, targeted suites green — 137 db + 21 config-worker + 12 agents-worker + 11 console tests): **contracts** `packages/contracts/src/agents.ts` — `AGENT_PROVIDERS` grows `openai`/`openrouter`; new `COMPUTE_PROVIDERS`/`MODEL_PROVIDERS`/`isModelProvider`; `config` documented as `{defaultModel?, baseUrl?}` for model providers. **db** `packages/db/src/agents/model.ts` — `PROVIDERS` widened + `MODEL_PROVIDERS`/`isModelProvider`; migration **`860_agents_model_providers`** (idempotent DO block: drop + re-add the `provider_connections` provider CHECK with the four-provider vocabulary) registered in `manifest.ts` (sha256 checksum) and `infra/db-migrate/migrations.lock` regenerated (87 ids). **config-worker** `internal-provider-keys.ts` — the reserved-namespace regex widened to `(daytona\|anthropic\|openai\|openrouter)` with a mirror-both comment; custody path otherwise untouched (still the only decrypt path — key-hierarchy guard green). **agents-worker** `verifiers.ts` — OpenAI-compatible `GET /models` Bearer ping and OpenRouter `GET /key` ping, both honoring `config.baseUrl` (trailing-slash-trimmed), reasons redacted to status codes; `handlers/providers.ts` validation message now derives from `PROVIDERS`. **console** `lib/agents/model.ts` — `PROVIDER_META` cards for OpenAI/OpenRouter + `MODEL_PROVIDER_SET`; `provider-connections.tsx` — model-provider connect form gains optional Base URL + Default model fields (empty fields omitted so vendor defaults hold; key still never lingers in state). **tests** — `gemini` replaces `openai` as the rejected-provider fixture in `tests/config-worker/src/internal-provider-keys.test.ts` and `tests/db/src/agents-repository.test.ts` (openai/openrouter now assert as valid). **Completed in the DX6 finish slice:** `Settings › AI providers` (`/orgs/:slug/settings/ai-providers`, Developer group beside the MCP server) renders the same `ProviderConnections` component — three doors, one surface, zero new API; the profile dialog's model picker is **connection-aware** (`modelOptions()` merges `AGENT_MODELS` with every VERIFIED model connection's `config.defaultModel`, provider-labeled, deduped; unverified/non-model/empty skipped — tested). Verified: console 742 tests + typecheck + lint + full OpenNext build. **Still out (DX-Q6):** the OpenAI-compatible chat `ModelClient`; live verify smoke against real OpenAI/OpenRouter keys. |
| DX7 — Delegation interfaces (anthropic-managed) | 🗓️ Not started (design normative in `design.md` §10) | — |

## Notes for the first implementer

- Start at DX0 and get the fold *shape* right against fixtures before any DO
  exists — the live layer (DX1) is worthless over a wrong fold.
- Reuse, do not rebuild: the work fold (WP), the AG7 session list, the AF6
  attention feed, and the AF9 budget read are all shipped and viewer-scoped.
  The facade composes them; it owns no table.
- The `DispatchIndex` belongs in `apps/chat-worker` (unprivileged) as a sibling
  SQLite DO to `ChatIndex`/`WorkspaceMemory` — mirror their binding/migration
  idiom (per-env DO blocks, top-level migration).
- The dispatch head socket is attach v1 plus two frames; do not introduce a
  second sync vocabulary (AN decision 2).
