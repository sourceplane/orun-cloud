# saas-agents-native — Implementation status (as-built)

Status: **Complete (v1)** — epic authored and shipped 2026-07-17, AN0–AN7 merged (PRs #481–#487 here, orun #541/#542). Recorded tails: the chat-agent PRINCIPAL (unlocks AN5's standing head + AN6's proactive plane/scheduled briefs/routine targeting), work-plane write tools (unlocks AN5's write set), AF9 budget-envelope folding of `agents.chat_tokens`, live-model quality evals + staging verification (need credentials), and the KV `SessionRelay` class deletion one release post-cutover (lock 7).

| Milestone | Status | As-built notes |
|-----------|--------|----------------|
| AN0 (orun) | ✅ Shipped | In `orun` (PR #542): the body wire — one outbound WS at `…/wire` carrying inputs down (push latency) and acks + deltas up; durable `/events` batches keep the confirmed HTTP carriage (amended decision 1a in the orun spec); preferred-with-fallback, rotation re-dial. The cloud door it dials landed here with AN2. |
| AN1 | ✅ Shipped | **The relay on the SDK.** `agents@0.17.4` adopted in `apps/agents-worker`. `src/attach-relay.ts`: `AttachRelay extends Agent` — hibernatable WS head attach (`onConnect` = hello → replay → live, `onMessage` = head inputs with edge-stamped principal + in-socket acks, `onClose` = presence), the AL6 body routes + SSE fallback byte-identical in `onRequest`; identity chatter off (`sendIdentityOnConnect: false` — the wire is attach v1 only, lock 2). `src/relay-shell.ts`: the transport-agnostic glue (vendor-free, structurally typed) both jest and the DO share — WS sink, connect/rejoin/message handling, the HTTP surface; `RelayCore` carried verbatim **plus one additive method** `rejoin(sink)` (hibernation wake re-registration — no hello/replay, presence-only; a closed relay answers bye). `src/relay-epoch.ts` + env (`ATTACH_RELAY`, `RELAY_CUTOVER_AT`): session-epoch routing (lock 7) — new sessions on the SQLite SDK class, pre-cutover sessions drain on `SessionRelay` (WS upgrade on a draining session refuses 426 → client falls back to SSE); wrangler v2 migration `new_sqlite_classes: ["AttachRelay"]`, both bindings per env. **The AL6 remainder closed:** `handleIngestSessionEvent` now mirrors accepted batches to the relay DO as attach-v1 event frames (best-effort — DB stays the record); the budget interrupt rides the same epoch routing. Head attach/input routes now 404 unknown sessions (no phantom DOs). Conformance: golden fixtures driven through the shell over fake WS connections and the SSE sink produce **byte-identical frame logs**; WS input acks byte-equal the HTTP POST path's; hibernation rejoin shows no loss/duplication/second-hello; agents-worker suite 189 green, typecheck/lint/build green. **Deviation noted:** handlers keep `idFromName` (not `getAgentByName`) so route modules stay vendor-free and jest-testable — same resolution semantics. Live staging drain exercise rides the deploy. |
| AN2 | ✅ Shipped | **The console on the socket + the body-wire door.** Console: `lib/agents/attach-live.ts` (pure fold: replay/dedupe/cursor/delta-supersession/presence/bye — 5 tests) + `lib/agents/attach-socket.ts` (`useAttachSocket`: WS to the api-edge attach facade, backoff reconnect, cursor resume, EventSource fallback after 2 failed dials); `session-detail.tsx` folds the tail into the conversation (DB initial load + live frames deduped by seq), streams the in-progress turn, shows presence chips + the transport in the kicker; **`LIVE_POLL_MS` and the 5s poll are deleted**. api-edge: WS upgrade pass-through on agents routes (actor stamped, no idempotency layer, auth header never forwarded); the attach route accepts an `access_token` query bearer for browser WS/EventSource (attach-only, stripped before forwarding). **The body-wire door (AN0's counterpart):** `GET …/sessions/{id}/wire` (session-gated, upgrade-only, SDK-class-only) → `RelayShell` in agents-worker — head inputs pushed to connected wires at enqueue time (principal-stamped), unacked backlog re-pushed on wire connect, acked-ref set persisted across hibernation, wire acks/deltas/bye flowing back through the core; the long-poll still serves everything (the wire is an accelerant, not the record). Tests: +6 shell wire, +5 facade upgrade/token, +5 fold; suites green (agents-worker 195, api-edge 501, console 726). **Deviation:** the console head is a plain WebSocket + pure fold, not `useAgent` — resume is the attach cursor and sync is off (design §3), so the SDK client protocol would only add chatter. |
| AN3 | ✅ Shipped | **Lifecycle in the object.** `AttachRelay` gains `this.schedule()` timers: `armLease(orgId, lease)` re-armed by **every heartbeat** (`handleSessionHeartbeat` → typed RPC, best-effort — a miss belongs to the backstop); `onLeaseLapse` re-reads control-plane truth via `relay-lifecycle.ts#reportLeaseLapse` — still-live lease → re-arm; terminal/missing → stand down; genuinely lapsed → reclaim through the **shared sweep path** (`reclaimSession` extracted from sweep.ts — the DO reports, the transition table decides). A terminal `state_changed` in the ingest mirror cancels the lease timer and arms **retention GC** (`onRetentionDue`, 7d): the object purges its own KV mirror (the DB log + sealed snapshot are the record). The global cron **demoted to backstop auditor** — unchanged scan, expected zero; a nonzero find logs `BACKSTOP FINDING`. **Typed RPC (the trench coat comes off):** `relayPeerFor` resolves kind — the SDK class gets method calls (`initSession`/`ingestEvents`/`streamDelta`/`pollInputs`/`ackInput`/`headInput`/`armLease`), the draining KV class keeps the HTTP forward (deleted with it, lock 7); WS/SSE upgrades legitimately stay `fetch` (they move a live Request). Tests: 7 lifecycle (lapse decision × 4, heartbeat arming × 2, RPC routing), mirror tests re-pinned to RPC; agents-worker suite 203 green. |
| AN4 | ✅ Shipped | **The Workspace Agent — the voice.** New `apps/chat-worker` (component + wrangler v1 `new_sqlite_classes`): `WorkspaceAgent` DO per thread (`chat:<chatId>`, hibernatable) + `ChatIndex` DO per workspace (thread registry — no control-plane DB, the worker stays unprivileged). The conversation core is `chat-thread.ts` (pure, storage-injectable): durable message history, WS head replay→live with cursor resume (a second browser resumes mid-stream), the model turn loop with tool rounds, **immutable workspace binding** (a thread never migrates orgs), one-voice-per-thread 409, and the **honest retryable error turn** on custody/model failure. Model: `@anthropic-ai/sdk` streaming (`claude-opus-4-8`, adaptive thinking) behind a `ModelClient` seam — jest drives **recorded-model fixtures**, never the network. Custody (lock 6): connection discovered via the PUBLIC providers surface with the owner's bearer, key resolved per turn through the CONFIG_WORKER seam (`providerSecretRef` convention, conformance-tested against `@saas/db/agents`), never stored. Toolset v1: the `@saas/mcp` registry's **read-only set consumed in-process**, executed with an owner-credential SDK against api-edge (deviation from "MCP client over Streamable HTTP": same tools, same handlers, same public-surface re-entry, one less hop) — **CI-asserts** no write-capable tool is reachable. Authz: `organization.agent.chat` registered (contracts + policy-engine owner/admin/ops), deny-by-default via POLICY_WORKER. api-edge: chats facade → CHAT_WORKER; the turn route (and only it) forwards the owner bearer as `x-owner-bearer`; the chat WS accepts the browser query bearer (stripped). Console: `/orgs/{slug}/agents/chat` thread list + thread view (chat-v1 fold `chat-live.ts`, WS + cursor resume, tool cards, streaming line). Tests: 19 chat-worker (fixtures/custody/router/read-only CI gate) + 4 facade + 4 fold; suites green (chat 19, agents 203, api-edge 505, console 730, policy 234, contracts 172). Deviations recorded above + `Agent` (not `AIChatAgent`) with own chat-v1 frames — the platform head idiom, fixture-testable. |
| AN5 | ✅ Shipped (v1) | **The hands.** `session-verbs.ts` in chat-worker: four verbs merged beside the read-only platform tools, all re-entering PUBLIC surfaces with the chat owner's credential (lock 4). `session_spawn` → the AG9 **dispatch door** (entitlement, ladder, spawn gates, budget door apply server-side); **a refusal renders its reason verbatim in-thread** (tested with a budget refusal). `session_steer`/`session_interrupt` → the AL input route with an explicit `via: workspace-agent` **disclosure in the frame payload** — the sealed log shows the authenticated owner AND agent authorship (deviation from "attributed to the chat agent as principal": the agent holds no principal of its own yet; the disclosure rides the frame). `session_watch` → folds the child's state + recent events + **pending approvals** into the thread (approval cards say WAITING ON A HUMAN and carry the session link). **Lock 5 structural + CI-pinned**: no verdict verb exists; the input door emits steer/interrupt frames only; suite asserts nothing verdict-shaped is expressible. Autonomy: the spawn tool's contract instructs propose-confirm-then-spawn at `assist` (ladder enforcement itself stays at the dispatch door). **Deliberately deferred, recorded**: (a) the STANDING head on the child relay (presence `surface: workspace-agent`, push-latency child cards) needs a chat-agent principal for standing credentials — the owner bearer is per-turn by design; rides the AN6/AN7 tail. (b) Write-tool enablement: the design named `task_create`/`task_comment`/`contract_propose`, but the shipped MCP5 write set is infra writes (project/env/flag/webhook/member) — **spec drift caught by implementation**; the chat toolset stays read-only + verbs until work-plane write tools exist. Tests: +8 (verb doors, refusal rendering, disclosure, pending-approval fold, lock-5 structure); chat-worker suite 27 green. |
| AN6 | ✅ Shipped (memory plane; proactive plane deferred) | **Memory.** `WorkspaceMemory` DO per workspace (`wsmem:<orgId>`, SQLite, wrangler v2 migration): PROVENANCED entries — content · source ref (`chat:<chatId>`) · author · timestamp. **No hidden memory**: writes happen only through the visible `memory_remember` tool (thread card confirms exactly what was kept, attributed to the authenticated user) — the tool's contract restricts it to explicit user asks. **Brief assembly**: every turn folds the plane into the system context (`formatMemoryForSystem`, capped at 50 entries) — a remembered fact demonstrably shapes later turns and a deleted entry stops (tested); a memory outage degrades the brief, never the turn. Console: `/agents/memory` page — list, inspect, **working provenance links** (chat/session refs resolve to their surfaces), edit, forget. Routes (chat grant, deny-by-default) through the api-edge chats facade; SDK `listMemory/updateMemory/deleteMemory`. Also fixes the AN4 path bug (console page wrappers committed under `packages/sdk/apps/…` — moved to `apps/web-console-next/…`). **Proactive plane deferred, recorded**: scheduled briefs/digests and `target: workspace-agent` routines need a chat-agent principal (scheduled work has no owner bearer by design — the same credential tail as AN5's standing head); the `this.schedule()` substrate is already on the class. Tests: +7 (brief fold with/without entry, cap, visible write + provenance, honest failure, route authz + CRUD incl. delete-then-404); chat-worker suite 34 green. |
| AN7 | ✅ Shipped (v1) | **Trust.** **Eval harness** (`tests/chat-worker/evals.test.ts`, CI-gating): STRUCTURAL injection-regression suites — a compromised-model fixture (a model that obeys hostile content) attempts (a) **verdict forgery** via four verdict-shaped tool names → every one refused, zero public-door traffic; (b) the full reachable toolset audited: platform write tools absent, only the four verbs + visible `memory_remember` beyond the read set; (c) **memory poisoning** → the write can only land as a VISIBLE provenanced entry (thread card + author + source; the brief fold carries attribution — a poisoned entry is attributed content, never an invisible instruction); (d) **spawn escalation** → hits exactly the public dispatch door where the gates live, never around it. (Model-QUALITY evals — grounding, refusal correctness — need a live key; recorded as the credentialed staging gate.) **Metering:** the model seam now reports usage; `agents.chat_tokens` emits per turn through the PUBLIC usage ingest with the owner's credential (fire-and-forget), and the closing assistant message carries the turn's tokens (thread-level cost visible to the user). Budget-envelope folding of chat spend into AF9 ceilings deferred (control-plane fold; the meter is the substrate). **Observability:** a per-turn `[chat-turn]` trace line (chat, org, ok/reason, tool count, tokens, ms — shape, never content). **Hardening:** per-thread turn rate ceiling (20/5min sliding window, honest `rate_limited` → 429); thread **deletion is complete** (`DELETE /agents/chats/:id` → DO storage purge + registry removal + 404 after; the DO's storage IS the thread, so deletion ≡ retention ≡ export scope); SDK `deleteChat`. Tests: +11 (4 forgery names, roster audit, poisoning visibility, escalation path, rate window incl. slide, metering visibility, deletion flow, 429 mapping); chat-worker suite **45 green**. |

Pre-existing substrate this epic builds on (shipped elsewhere, recorded here
for orientation): the AL6 relay as-built (`relay-do.ts` + `relay-core.ts` +
attach-v1 contracts/fixtures), the AL7/AL8 console head + edge attribution,
AG5–AG9 provisioning/dispatch/autonomy, AF4–AF9 gates/routines/budgets, and
the MCP0–MCP10 platform tool plane. See each epic's own
`IMPLEMENTATION-STATUS.md` for their as-built truth.
