# saas-agent-supervision — Implementation Status

**Status: SV0–SV2 landed; SV3 core landed; SV4 landed.** Design complete (this
doc set). This file is the as-built record as SV0+ land — deltas from
`design.md`, recorded amendments, and tails, in the house convention.

| Milestone | Status | PRs | Notes |
|-----------|--------|-----|-------|
| SV0 — Origin taint | **done** | (this PR) | `origin {kind,ref?,label?,backfilled?}` on `AgentSession` (contracts + db model) and `agent_sessions.origin` JSONB (migration `920_agents_origin`, NOT NULL default `{kind:human}`, backfill infers parent⇒session / routine⇒routine / work⇒work / else human, all `backfilled:true`; expression index `(org_id, kind, ref)` backing the SV1 fold). Recorded at the AG9 door from the authenticated caller's context — never a body `origin` field. **Delta from design §2.2:** the door cannot structurally tell a Work-surface dispatch from a dispatcher-thread dispatch (both hit `POST /dispatch` with `{taskKey}`); resolved by a narrow first-party hint — chat-worker's `session_spawn` stamps `dispatchRef` (its `ch_…`) + `dispatchLabel`, its presence ⇒ `dispatch`, absence ⇒ `work`. Origin carries no authority (nothing gates on it), so the hint is cosmetic-only; `session`/`routine`/`human` stay door-authoritative and unforgeable via the body. **Delta:** backfill also infers `routine` (design §2.2 listed session/work/human only) — a `routine_id` row is more truthfully `routine` than `work`/`human`. Chip renders on fleet rows (non-linked — the row is already a link) and the cockpit header (deep-links to thread/work/parent). |
| SV1 — The roster fold | **done** | (this PR) | `GET …/agents/chats/:chatId/implementers` — a per-viewer, `session.read`-gated fold over sessions where `origin={kind:"dispatch", ref:chatId}`, split active/terminal, each active entry carrying its delegation tier (from the profile `interface`) and the AF6 needs-you fact (reused via `foldAttention` over the thread's own sessions). Returns `{chatId, active[], running, needsYou, done}` — the numerals the panel and the SV7 roll-up both read (one truth). SDK `agents.chatImplementers`. Console: `RosterPanel` side rail on the thread page (`workspace-chat.tsx` → two-column grid), snapshot-first + `refetchInterval` poll (the #607–609 liveness discipline), cards deep-link to the cockpit; terminal implementers fold to the `done` count only (they live on Implementers, SV4). **Delta from design §7.2:** "last-event age" is approximated by `startedAt ?? createdAt` (same as the fleet row) rather than a per-session events read — a precise last-event timestamp can come with SV3's digest. |
| SV2 — The dispatcher principal | **substrate landed** | (this PR) | The **`agent_dispatcher`** service-principal role (policy-engine): grants exactly `organization.agent.session.read` / `.create` / `.spawn` / `.interact` and nothing else — no secret/config/settings/work-mutation, no verdict (none exists). Bindable onto a service principal (policy + membership-worker SP-binding allowlists) but **deliberately absent from `ORGANIZATION_ROLES`**, so no human can be invited or assigned it (all human role paths gate on that array). Cockpit **attribution** (SV2 §3/D): the `via: workspace-agent` disclosure — already stamped by the AN5 verbs but previously **dropped by the console** — is now threaded through both transcript folds (`session-lens.tsx`) and `TranscriptItem`, and `TranscriptRow` renders a `via==="workspace-agent"` steer as **"Workspace Agent · steer"**, distinct from a human steer, with the principal (owner on a human-prompted turn, the dispatcher `sp_` on a supervisor turn) beneath. Attribution is proven at the relay: a steer stamped with a dispatcher `sp_` seals to that principal while the disclosure is preserved; a human-prompted one stays owner-attributed (`relay-core` fixtures). **Re-slice from design §3:** the *lazy mint* of the per-workspace dispatcher principal + the session-verbs **credential selection** (owner bearer vs dispatcher bearer per turn) land in **SV3**, where supervisor turns first present the credential — building them here would be dead until then, and the sequencing already puts enablement in SV3. SV2 lands the grant, the attribution wiring, and the relay proof (all live/tested now). Authz denial tests pin the ceiling; the relay stamps the authenticated actor, so no relay change is needed for correct attribution. |
| SV3 — The supervision loop | **core landed (loop is pure + fixture-tested; live wiring staged)** | (this PR) | The wake/digest/guard logic — the design's "pure and fixture-tested" heart (§4). **Contracts:** the closed `WAKE_KINDS` set (terminal / approval / budget / child / stuck), `ALWAYS_WAKE_KINDS`, `DigestEntry` + `SupervisionDigest` (bounded by `DIGEST_ENTRY_CAP`), `SUPERVISION_MODES` (on/observe/off), the SV3 card registry + `EscalationCard` (no verdict field, by construction). **Pure engine** (`apps/chat-worker/src/supervision.ts`): `wakeKindForEvent` (classify), `buildDigest` (coalesce → dedupe by (session,seq) → drop reflexive → keep terminal/approval always, cap the rest with overflow), `isReflexive` (dispatcher-caused non-terminal drops; terminal/approval always ring), `WakeAccumulator` (the 5s coalescing window with `msUntilDue` for the DO alarm), `escalationsFrom`, `supervisionRunsModel`. **Supervisor turn** (`ChatThread.runSupervisorTurn`): a turn with **no human prompt** — seals a `supervisor:true` marker on every message; the digest+roster enter as a synthetic user message framed as **untrusted** data (§9.2); shares the human turn-rate ceiling (never starves a human); `observe` mode seals the marker and runs **zero** model calls (the cost dial); the tool roster is the caller's (human roster minus `ui_`; no verdict verb exists). The `runTurn`/`runSupervisorTurn` model loop is extracted into one shared `modelLoop`. **Tests:** N events → 1 digest → 1 turn; storm (100/window) → one bounded digest; reflexivity (dispatcher steer echo) → no wake, terminal/approval always ring; observe → zero model calls; approval → escalation card with no verdict; shared rate ceiling. **Staged (the live half — gated by the design as "chat smoke live-key only" and blocked on infra the codebase marks as future):** (a) the **ES-lane doorbell consumer** that routes dispatch-origin session events to `chat:ch_x` and arms the window alarm — the `DispatchIndex` doorbell only self-rings on chat turns today (dispatch-index.ts / dispatch-core.ts comment it as future; the consumer needs an origin lookup that only agents-worker/events-worker can do); (b) the **AF9 budget burn + park** — there is no thread-tree budget grain and chat-worker has no budget access (heaviest lift; needs a new grain + a public seam + a park state); (c) **per-thread mode persistence + UI toggle**; (d) the **SV2-deferred SP mint + credential selection** (a custody-stored API key resolves to the dispatcher `sp_` actor — the clean path — needs an identity binding + a config-worker `store` client). These compose onto the pure engine without changing it. |
| SV4 — The IA (Agents / Implementers) | **done** | (this PR) | **Nav rename** (`nav-items.ts`): the org-root landing "Dispatch" → **Agents** (the dispatcher thread-first surface — org root already renders the thread-first `DispatchSurface`), and the fleet page "Agents" → **Implementers**. It is a pure LABEL change — no route moves — so no deep link breaks (the `/dispatch` alias already redirects to root; `/agents/[sessionId]` + `/agents/chat/[chatId]` stay put, which `originChip`, `SessionRow`, and the roster all depend on). **Cmd-K** gains `nav.agents` + `nav.implementers` with synonyms (dispatch / fleet / sessions) so old muscle memory finds the renamed surfaces. **Implementers facets** (`agents-workbench.tsx`): a filter bar (northwind `Chip`/`ChipRow`, the IX1 hub idiom) over the full tainted list — origin kind (only kinds present render), infra state (active / terminal), interface tier (Sealed / Managed), and needs-you; the header + copy become the "full tainted fleet" language and terminal runs render under a **Terminal** section (they live here, and only here — Agents shows active). The matching is pure (`sessionMatchesFacets`, `presentOriginKinds`, `facetsActive` in `lib/agents/model.ts`) so it is unit-tested (no render harness in web-console). **Supervised-by banner** (`session-detail.tsx`): a dispatch-origin implementer shows "Supervised by ⟨thread⟩ · Open thread →" deep-linking `/agents/chat/${origin.ref}` (reusing the `ProvisioningNotice` idiom + the SV0 origin chip already in the header). Active-only-on-Agents invariant holds by construction (the SV1 roster fold already returns `active[]` only; terminal folds to the `done` count). **No route move** — the design's "301/redirect · no dead links" is satisfied trivially since nothing moved. **Tests:** nav labels (Agents/Implementers), facet predicates (origin/state/tier/needs-you, AND-combine, present-kinds), Cmd-K registry. |
| SV5 — Takeover (control protocol) | not started | — | — |
| SV6 — Executor-agnostic supervision | not started | — | — |
| SV7 — Foreman brief + hardening | not started | — | — |
