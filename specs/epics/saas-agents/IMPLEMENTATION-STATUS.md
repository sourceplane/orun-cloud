# saas-agents — Implementation Status (as-built)

The as-built record, kept distinct from the design/plan docs. The runtime half
(AG0–AG4) is complete in `sourceplane/orun` (`orun/specs/orun-agents/`); this
file tracks the orun-cloud control plane (AG5–AG12).

| Milestone | Status | As-built |
|-----------|--------|----------|
| AG5 — Sandbox provider plane | 🏗️ Foundation shipped | **Dormant substrate landed** (the OP0 posture — everything typed/migrated, nothing serving). `packages/contracts/src/agents.ts`: wire types (`AgentProfile`/`AgentSession`/`AgentSessionEventWire`), closed vocabularies (session states, run kinds, autonomy, the 11-kind event vocab — no status kind), error codes, and the **`SandboxProvider`** + `SandboxSpec`/`SandboxRef` seam (types only; dependency-free). `packages/db/src/agents/`: `model.ts` (vocabularies + guarded transition table + validators — mandatory responsible owner), `types.ts` (`AgentsRepository`), `repository.ts` (Postgres over the schema; `FOR UPDATE` transition guard, `ON CONFLICT (session_id,seq)` relay dedupe), `memory.ts` (test double + executable contract), `index.ts`; migration **`650_agents_foundation`** (schema `agents`: `agent_profiles`, `agent_sessions`, `session_events` relay, `autonomy_policies`; CHECK'd vocabularies, partial lease index, workspace-scoped) registered in `types.ts`/`manifest.ts`/`package.json` + lock. `apps/agents-worker/`: dormant Cloudflare Worker (`/health` only, component.yaml, wrangler template + fixture, bindings declared for AG6). Verified: db typecheck + 855 db tests (incl. 7 new agents-repository tests) + lint green; worker typecheck + `wrangler deploy --dry-run` (bindings resolve) + lint green; contracts typecheck + lint green. **Live slice shipped** (post-AG12, BYO-credentialed): `apps/agents-worker/src/providers/daytona.ts` — the Daytona `SandboxProvider` adapter over the workspace's own account (create from `agents-base` snapshot with Bearer auth + `ttlSeconds → autoStop/autoDelete` provider reclaim; toolbox exec; suspend/resume as stop/start; force destroy; health from sandbox state; every failure redacted to a status code). `handlers/provision.ts` + `POST /agents/sessions/{id}/provision` (router + api-edge facade): the spawn gate resolves the workspace's `daytona` + `anthropic` connections (sole-or-`default` selection, design §10.4), refuses loud at the gate on missing/unverified/invalid (`provider_connection_invalid`, session stays `requested` and retryable), resolves both keys through AG12 custody, boots the sandbox with **non-secret create-time env** (`ORUN_SESSION_ID/ORG_ID/RUN_KIND/TASK_KEY`), starts `orun agent serve` with `ANTHROPIC_API_KEY` on the **exec env only** (never survives suspend), records `{provider,id,connection}` in the session's sandbox JSONB, and advances `requested → provisioning`; provider failure destroys the half-booted box and lands `failed` with a redacted reason. `SandboxFactory` seam on deps keeps tests binding-free. Tests: 7 adapter + 11 provision-flow (agents-worker suite now 40). **Remaining:** live verification against a real Daytona account (needs test keys), the base-snapshot publish, and the `local-docker` dev adapter (runtime-side). |
| Live-integration fixes | ✅ | Surfaced connecting real providers: (1) **org-id decode** — agents-worker forwarded the public `org_<hex>` id straight to membership/policy (where `asUuid` throws → `{ok:false}` → 403 "Not authorized") and to `org_id UUID` columns; now decoded to a UUID at the router boundary like every other worker, with the public id reconstructed for the runtime-facing `ORUN_ORG_ID` env, `work://` refs, and session-token orgId claim. (2) **policy grants** (#397) + **policy-worker redeploy** (#398). (3) **create-profile console dialog** (#399) + copy clarifying that an API key *is* the service principal a profile binds. |
| AG6 — Session identity + DO relay | 🏗️ Control plane shipped | `apps/agents-worker` lit up from dormant: workspace-scoped routes (`/v1/organizations/{orgId}/agents/profiles` · `/agents/sessions` · `/agents/sessions/{id}` · `/agents/sessions/{id}/events`) over the AG5 `AgentsRepository`. Handlers (profiles, sessions, events) authorize deny-by-default through the membership + policy workers (`agent.profile.*` / `agent.session.*` actions), actor-gated (api-edge sets `x-actor-*`), returning the `@saas/contracts/agents` wire shapes; `spawnedBy` is taken from the actor, not the body. A `deps` injection seam (repo + authorize + dispose) lets `route()` be driven with a `MemoryAgentsRepository` + stub authorizer and no live bindings (the create-setting.ts pattern); production builds a Postgres repo from `PLATFORM_DB` + the authz clients. Tests: `tests/agents-worker` — 10 route tests (health, 404/401/403/503/405, profile create+list+validate, full session create→get→advance→events). Verified: worker + tests typecheck + lint; `wrangler deploy --dry-run` green. **Credential chain + dial-home shipped** (design §3.2–§4): identity-worker mints the **agent-session token** — same HS256 envelope as CLI/workflow tokens (one signing key, one bearer path), `actorKind: agent_session`, subject = the profile's service principal, `sessionId` claim for audit; kind-isolated (never verifies as user/workflow). `resolve-bearer` resolves it to a plain **service_principal** actor with `agentSession.id` surfaced — no new identity plane. Internal mint route `/v1/internal/identity/agent-session-token` (service-binding-only; validates the `sp_`/`as_` triple so it never launders arbitrary subjects). api-edge forwards the binding as `x-actor-agent-session-id` — set ONLY from the resolved bearer, spoofed inbound headers dropped. agents-worker runtime routes, all behind the **three-way gate** (service principal + the session's own principal via `getSessionProfile` join + token bound to THIS session): `POST /sessions/{id}/heartbeat` (first beat flips provisioning → running; then `touchSessionLease`, which refuses terminal states), `POST /sessions/{id}/events` (batch ingest ≤100, idempotent on seq, closed vocabulary), `POST /sessions/{id}/token` (**lease-gated refresh** — lapsed lease or terminal state refuses, so kill = never extend and a runaway credential dies within one TTL). Provision now mints the first token and injects `ORUN_SESSION_TOKEN` on the exec env beside `ANTHROPIC_API_KEY` (mint failure = 502 before any provider call). Tests: identity 20 (mint/verify/kind-isolation/resolve/route validation), agents-worker 9 runtime + provision updates, db lease/join, api-edge binding-forward + anti-spoof. **Lease sweep shipped** (design §4.3): a `*/5` cron in agents-worker reclaims the fleet cross-org — running/awaiting_approval sessions whose lease lapsed past a 5-min grace, plus provisioning sessions stalled 30 min (they never heartbeat, so they never earn a lease) — destroying each sandbox on the workspace's own Daytona account (best-effort: a destroy failure, a rotated key, or a disconnected account never blocks the reclaim) and landing the session `failed(lease_lost)` with the sandbox ref kept for audit. `listLapsedSessions` is the one deliberately CROSS-ORG repo query; the scheduled handler no-ops when unbound (dormant posture preserved). 6 sweep tests + cross-org query test. **Remaining AG6:** per-session DO relay (R2 mirror + SSE live tail), suspend/resume + snapshot retention, IG4 repo tokens, SM3 `how: agent-session` resolve. |
| AG7 — Console Agents tab | ✅ Shipped (fleet view + providers; live SSE tail rides the DO relay) | `@saas/sdk` gains the **agents resource** (profiles, sessions + provision + relayed events, provider connections — URL-shape + write-only-key tests; no method can read a key back). Console: **Agents** joins the product rail beside Work (`/orgs/{slug}/agents`, Bot icon) — the fleet view: provider cards on top (connect Daytona/Anthropic = paste key, verified/invalid pill with `…last4` hint + redacted reason, verify/disconnect; the "add your AI provider keys" affordance exactly where sessions spawn), hosted-session rows (state pill with live dot, run kind, task key, spawner) linking to the **session detail** (infra facts, task/PR/sealed-snapshot pills, relayed session log; polls at 5s while live, stops on terminal states — the SSE tail replaces the poll when the DO relay lands), and the profiles list. The **Integrations hub** renders the same `ProviderConnections` component under "AI & compute providers" (two doors, one surface, design §10.5). Presentation model (`lib/agents/model.ts`) is pure + tested (every closed-vocabulary state maps). Tests: console 540 (agents-model + nav), sdk 205 (agents URL shapes); console build green. |
| AG8 — Design runs | 🏗️ Spawn path shipped | The Work tab is where sessions are born (design §6): spec headers gain **"Design with agent"** and task rows a hover-revealed **"Agent"** action, both opening the shared `SpawnAgentDialog` — profile picker, run kind fixed by the entry point (spec → `design`, task → `implementation`), the item key carried as `taskKey` plus a `work://{org}/{key}` provenance pointer (`workRefForItem`). Spawn = `createSession` → (default-on) `provisionSession`; a spawn-gate refusal (no verified Daytona/Anthropic) surfaces inline with a pointer to the Agents tab and the session stays `requested`, retryable after connecting. Zero work-plane writes — spawning never touches a rung. **Remaining AG8:** the in-sandbox design deliverable (epic-files PR + `contract_propose` via the four agent tools — the orun runtime side is shipped; needs the live fixture flow), proposed-contract review UI beside the spec PR. |
| AG9 — Dispatch + autonomy | 🏗️ Ladder + dispatch door shipped | Autonomy policy routes (`GET/PUT /agents/autonomy`, actions `organization.agent.autonomy.read\|write`): the ladder (`manual → assist → auto-dispatch → full`) per workspace with per-spec overrides + caps, effective level surfaced with the inherited default so the console renders "inherited" honestly. **The one dispatch door** (`POST /agents/dispatch` — the Work-tab assignment hook, the later ES1 lane consumer, and "Dispatch all Ready" all re-enter here): gate 1 autonomy ≥ auto-dispatch (spec override wins), gate 2 one-live-run-per-task dedupe, gate 3 concurrency cap (`caps.maxConcurrent`, default 3); profile = explicit → sole → `impl-default` convention; spawn carries `taskKey` + `work://` provenance and boots through the same AG5 provisioning path — a provider-gate refusal returns 201 with the session **parked as `requested`** (`provisioned: false`, gate reason in the payload), retryable after connecting. api-edge facade adds autonomy/dispatch + PUT (body forwarded on POST *and* PUT). Tests: 15 new (ladder default/override, non-ladder level 422, refuse-below-auto-dispatch, dispatch happy path with provenance, dedupe 409, cap 409, parked-on-gate, no-profile 404, methods) — agents-worker 64, api-edge 490. **Remaining AG9:** the ES1 lane consumer wiring (assignment events → this door; poll fallback), fold re-check against work truth (`ready ∧ unassigned`), per-task retry budget with park-and-comment, spec-level "Dispatch all Ready" fan-out UI. |
| AG10 — Metering + entitlement | 🏗️ Gate + emission shipped | **Entitlement**: dispatch gains gate 0 — `feature.agents` checked over a new BILLING_WORKER service binding via the established internal check seam (`x-internal-caller: agents-worker` joined billing's allow-list); the decision is **D3-open** like MCP6 (only an explicit plan `disabled` denies — `not_configured` and billing hiccups allow, so a billing outage never parks the fleet before the feature is priced), and the deny carries the upgrade path. **Metering**: a successful boot emits exactly one `agents.sessions_started` (dimensions: runKind + profile) through a new METERING_WORKER binding via the public usage-record contract — fire-and-forget, never blocks or fails a spawn, and a gate-parked dispatch emits nothing. Deploy DAG edges added for both bindings. Tests: 5 new (D3-open matrix, deny-with-upgrade, 403-at-dispatch, exactly-one-sample-on-boot, nothing-on-parked) — agents-worker 69; billing 161 + config-worker 357 guards green; wrangler dry-run resolves both bindings. **Remaining AG10:** `agents.session_minutes`/`agents.tokens` (need terminal-transition + relay cost samples — the DO relay slice), plan-tiered concurrency quota via `limit.agent_sessions`, U7 upgrade CTA in the console, cost on session detail + Usage rollup, lease-truth reconciliation replay test. |
| AG11 — Hardening + evals | 🗓️ Not started | — |
| AG12 — Provider connections (BYO Daytona + Anthropic) | ✅ Shipped (API; console cards ride AG7) | Workspace-connected provider accounts (design §10): `agents.provider_connections` (migration **`680`**; `UNIQUE (org_id, provider, name)`, CHECK'd provider/status vocabularies, `secret_ref` + last4 `key_hint` — **no key column**). `@saas/db/agents`: `ProviderConnection` model + `providerSecretRef()` (reserved `agents/providers/<provider>/<name>/API_KEY` namespace) + repository/memory connection methods (create/list/get/setStatus/delete; verified stamps `last_verified_at`). `@saas/contracts/agents`: wire shape + `CreateProviderConnectionRequest` + provider error codes. **Key custody**: config-worker gains two service-binding-only internal routes (`/v1/internal/config/provider-keys/store\|resolve`) gated by regex to the reserved namespace — config-worker stays the ONLY decrypt path (key-hierarchy guard test updated to enumerate it as the third authorized importer); plaintext lives only in the resolve response. **agents-worker**: `/agents/providers` (GET/POST) · `/providers/{id}` (DELETE) · `/providers/{id}/verify` (POST), authorized as `organization.agent.provider.read\|write`; create is custody-first (store fails ⇒ no row), then a read-only verification ping (`GET {daytonaApi}/sandbox` Bearer · `GET api.anthropic.com/v1/models` x-api-key) sets `verified`/`invalid` with a **redacted** reason (status code only); `ProviderKeyClient` + `ProviderVerifier` seams keep tests binding-free. **api-edge**: facade forwards the three provider routes (adds DELETE; body forwarded only on POST). Deploy DAG: agents-worker→config-worker and api-edge→agents-worker `dependsOn` edges added (the latter a latent AG6 gap the deployment-config guard caught). Verified: db 862 + config-worker 357 + agents-worker 22 + api-edge 487 tests green; typecheck + lint green across all five packages. **Remaining:** the spawn-time `resolve → ANTHROPIC_API_KEY` injection and the Daytona `SandboxProvider` adapter consume these connections (AG5/AG6 live slices — now unblocked by BYO keys); console Integration cards ride AG7. |
