# Epics

Status: Normative index

Orun-style work programs for the SaaS starter. Each epic is a folder named
`saas-<slug>` (mirroring `../../orun/specs/orun-<slug>`) carrying a canonical doc
set. The durable per-bounded-context contracts live one level up in
`specs/components/`; epics are the cross-cutting programs that *evolve* them.

## The epics

| Epic | Cluster | Status | Owner(s) | What it is |
|------|---------|--------|----------|------------|
| [`saas-baseline/`](./saas-baseline/) | **B** | In progress | all workers | Table-stakes a credible SaaS starter must own: real auth, notifications, idempotency/rate-limits, SDK/CLI, webhooks, billing UX, audit UX, admin, observability, SSO. |
| [`saas-console-ux/`](./saas-console-ux/) | **U** | In progress | web-console-next, packages/ui | The buyer-credible console: App Router on Workers, design system, URL-driven scope, Cmd-K, empty/skeleton states, upgrade UX, SDK client. |
| [`saas-performance/`](./saas-performance/) | **PERF** | In progress | api-edge, packages/* | Make authenticated reads feel instant — measurement record, root-cause analysis, and the PERF task ladder. |
| [`saas-resources-runtime/`](./saas-resources-runtime/) | **P2** | Draft (not started) | resources-worker, runtime-worker | The differentiator/moat: manifest-driven project resources + runtime orchestration. Consumes components `06` + `08`. |
| [`saas-multi-org-billing/`](./saas-multi-org-billing/) | **B** (billing platform) | In progress | membership-worker, billing-worker, db, console | Datadog-style multi-org: default single org, more orgs are a purchased capability, billing rolls up to the default/parent org. Carries the `billing-provider-abstraction` sub-epic (Polar first, Stripe/others by config). Extends B6 + B11. |
| [`saas-integration-tenancy/`](./saas-integration-tenancy/) | **IT** (bridges IG + MO) | Draft | integrations-worker, db, api-edge, console | One GitHub App installation serves a whole account: the **parent org is the integration tenant** via `effectiveIntegrationOrg` (twin of `effectiveBillingOrg`). The connection resolves up to the account; workspaces consume it for repo links + scoped tokens; inbound events project down to the owning workspace. Preserves the `installation_id` keystone — no constraint relaxed. Bridges `saas-integrations` (IG) and `saas-multi-org-billing` (MO). |
| [`saas-workspaces/`](./saas-workspaces/) | **WS** (vocabulary over MO) | Draft | web-console-next, api-edge, contracts/sdk/cli | Rebrand the sub-organization to a **Workspace** and present the parent as an **Account** that contains Workspaces. A **relabel, not a remodel**: a Workspace *is* an `organizations` row; depth is label + public-API aliasing (`/v1/workspaces/*` aliases `/v1/organizations/*`), internal `org_id` untouched. Rejects "Product" (Polar SKU collision) and "Project" (existing sub-unit). |
| [`saas-workspace-id/`](./saas-workspace-id/) | **WID** (durable id + account-layer seam over **WS**/**MO**) | Draft | db, membership-worker, api-edge, contracts/sdk/cli, console, identity-worker | Give every Workspace a short, immutable, public id (**`ws_`** + Crockford base32) — the durable handle `org_<hex>` (long) and `slug` (mutable) can't be — and sequence the **Account layer** from derived role (Stage 0) to owner-of-state/authority (Stage 1: scope-resolution chain + `scope_kind='account'` RBAC) to first-class entity (Stage 2, deferred). Additive: `org_<hex>`, the UUID PKs, `/v1/organizations/*`, and `org.*` events are all retained; `ws_…` is led-with. Role is **never** in the id — account-vs-workspace is discoverable via `accountId`+`kind`. |
| [`saas-teams/`](./saas-teams/) | **TM** (account-owned principals over **WID**) | Draft | membership-worker, db, policy-engine, api-edge, contracts/sdk/cli, console | Teams as account-owned **principals** (à la GitHub org teams), granted roles via the existing `role_assignments` (`subject_type='team'`) and expanded into facts at authorization-context assembly — **no** policy-engine rewrite, **no** tenancy remodel. Account-scope grants cascade to all (incl. future) workspaces. Explicitly **not** a hierarchy level (that is `saas-workspace-id` Stage 2). Builds on **WID6** (account RBAC, **shipped**). The **access-grant slice** of the `teams-*` program below. |
| [`teams-platform/`](./teams-platform/) | **TEAMS program** (over **TM**) | Draft (program index) | membership-worker, db, policy-engine, identity-worker, notifications-worker, state-worker, api-edge, contracts/sdk/cli, console | **World-class Teams** — promote Team from an RBAC principal into the product's primary organizing primitive across three planes (**access · ownership · collaboration**) **without a tenancy remodel**. Umbrella register indexing the five `teams-*` epics below; carries the two keystones (owner-handle→team **resolver** respecting the git-authored catalog invariant; thicken the account **surface**, not the tree). Rationale: the Account is only a *reference*, so Team — orthogonal to tenancy — is the connective tissue (à la Datadog Teams), not a hierarchy level. |
| [`teams-foundation/`](./teams-foundation/) | **TF** (entity over **TM**) | Draft | membership-worker, db, api-edge, contracts/sdk/cli, console | Promote Team → first-class **entity**: `team_` id + handle/profile, **team-level roles** (`team_admin`/`team_member`) for self-management, effective-access + **provenance**, `team.*` audit. The substrate the ownership/hub/collaboration planes bind to. |
| [`teams-ownership/`](./teams-ownership/) | **TO** (ownership grain) | Draft | membership-worker, state-worker, api-edge, contracts/sdk, console | The **keystone**: an account-authored **owner-handle → team resolver** (org metadata, **never** catalog content — `18-state` intact), catalog owner resolution + real `group-by: team`, and a **"My Teams / My Services"** lens across catalog + the flat Activities feed (delivers SC8). Ownership ≠ authorization. |
| [`teams-hub/`](./teams-hub/) | **TH** (hub + account surface) | Draft | web-console-next, api-edge, membership-worker, state-worker, contracts/sdk | The **Team Page** + thicken the account **surface** into an **Account Hub** (members · account-roles incl. WID6 list/revoke · teams · workspaces · usage) — no tenancy deepening. Cross-workspace **fan-out** read layer; multi-workspace team-grant management (the "add to N chosen workspaces from the account" action). |
| [`teams-collaboration/`](./teams-collaboration/) | **TC** (reach + on-call) | Draft | notifications-worker, notifications-client, membership-worker, state-worker, contracts, console | Team as **notification target** (`subjectKind='team'`, expand-to-members at enqueue, team-default + member-override prefs), `@team` handles, event→owning-team **routing** (via TO), and **team-scoped on-call/escalation defaults** (promote SC6, still *surface, don't page*). Rides **B2**. |
| [`teams-governance/`](./teams-governance/) | **TG** (enterprise) | Draft (mostly ⛔-gated) | identity-worker, policy-engine, membership-worker, contracts, console | **SCIM/SAML group→team** sync (⛔ on **B10**), **team-scoped restriction/visibility** (the allow-only-engine ceiling → a deliberate deny/ABAC decision), **custom roles**, and lifecycle governance (archival, ownership transfer, access reviews/attestation). The trust-at-scale plane. |
| [`saas-bootstrap-factory/`](./saas-bootstrap-factory/) | **BF** | Draft (not started) | stack-tectonic, infra/terraform/*, all wrangler surfaces, tooling/factory | Make the repo instantiable: kill hardcoded identity/resource IDs (config indirection + deploy-time wiring), complete the infra story (foundation, preflight, domain v5), then Blueprint/Instance contracts + instantiator + upgrade path. Acceptance test: a second live SaaS ("acme") from `instance.yaml` only. |
| [`saas-secrets-sync/`](./saas-secrets-sync/) | **SS** | Draft (SS0/SS1 in progress) | tooling/secrets-sync (new), tests/secrets-sync (new), stack-tectonic, infra/terraform/*, all wrangler templates | One write path for every secret: AWS Secrets Manager as system of record, deploy-lane sync into Cloudflare (no manual `wrangler secret put`), drift checks in verify lanes, Secrets Store for shared keys, rotation runbook. Extends the BF5/BF6 rails. |
| [`saas-product-experience/`](./saas-product-experience/) | **PX** | In progress | web-console-next, api-edge, notifications-worker, membership/projects workers, packages/{contracts,sdk,cli} | Close the backend-ahead-of-surface gap: console truth/papercuts, config/flags/secrets UI, notification preferences e2e, rename lifecycle, first-run onboarding, Cmd-K resource search. Opened from the 2026-06-11 verified-live audit. |
| [`saas-settings-ia/`](./saas-settings-ia/) | **SI** (console IA over **U**/**TH**/**TM**/**WID**) | Draft | web-console-next, packages/contracts (read-only roles mirror) | **Three scoped settings homes, one People & Access surface.** The console IA seam that makes navigation mirror the shipped `You → Account → Workspace` scope model: settings **follow the scope switcher** (SI2 three doorways — personal off the identity chip, Account off the Account chip, Workspace off the workspace chip), members·invitations·teams·roles·access collapse into one tabbed **People & Access** surface per scope (SI3, invitations become the Pending tab + inline role edit + access provenance), the four mis-scoped surfaces are re-filed (SI1 Billing→Account, Sessions/personal-notifs→You), Roles becomes a real permission-matrix destination (SI4), and the tenant/person "account" word-collision is ended (SI5 `/account`→`/you`). **Relabel & regroup, not remodel** — no tables, no tenancy change, `org_id` intact. Hosts the `teams-hub` TH1 Account Hub at a top-level doorway. |
| [`saas-integrations/`](./saas-integrations/) | **IG** (promotes P5) | Draft | new integrations-worker, api-edge ingress, db, contracts/sdk/cli, console | Pluggable integrations platform, GitHub App first: org-level install (connection bound to org), project repo links with branch→environment mapping, HMAC-verified inbound ingress normalized to `scm.*` events on event_log, and a token broker so tenant products act on GitHub without holding credentials. Provider seam from day one (GitLab/Bitbucket = adapter + config). |
| [`saas-event-streaming/`](./saas-event-streaming/) | **ES** (pays spec 09's router debt; consumes B2 + B5 + IG2; delivers the P3/P4 seams) | In progress — ES0 shipped (#325) | events-worker, notifications-worker, webhooks-worker, api-edge, db, contracts/sdk/cli, console | Datadog-grade event pipeline on the already-canonical `event_log`: typed event catalog in contracts, the spec-09 router as shared cursor **lanes** + dead-letter/replay, org/project **notification rules** (multi-segment globs · severity · attribute filters · throttle windows), a channel-provider seam with **Slack** first (credential-free incoming webhooks), dedup/correlation joining `scm.*` × `state.run.*` into single **event groups** (one story, not five pings), authenticated custom-event ingest, and a console Events explorer. No new worker (the router lives in the `events` context); no queues (the lane contract is the extraction seam). |
| [`saas-orun-platform/`](./saas-orun-platform/) | **OP** | Draft | new state-worker, identity-worker, config-worker, api-edge, db, contracts/sdk, console, infra | The SaaS becomes the Orun Platform (Orun Cloud): CLI session + OIDC auth, the remote state store (runs/objects/logs/catalog heads), secret manager with runtime grants, and the console's Runs/Stacks/Catalog/Secrets surfaces. One frozen wire contract shared with `orun/specs/orun-cloud/` (cluster **OC**). |
| [`saas-orun-backend-merge/`](./saas-orun-backend-merge/) | **BM** | Ready | state-worker, api-edge, identity-worker, db/contracts, infra; CLI side in `orun` (**NC**) | Replace `orun-backend`'s relational `runs/jobs/claim` plane with **native, event-sourced coordination** over the content-addressed store: a run is an append-only event stream rooted at `planDigest → sourceHash`, claims are conditional appends sharded **per run on a Durable Object**, Postgres is a delayed projection, and `job-result` objects give cross-tenant memoization. Greenfield (no permanent backcompat); **cross-repo** — pairs `orun/specs/orun-native-coordination/` (**NC**) on one vendored contract (`coordination-api.md`). `orun-backend` is the parity reference, not an API to keep. Extends **OP/OV**. |
| [`saas-service-catalog/`](./saas-service-catalog/) | **SC** | Draft | web-console-next, state-worker, contracts/sdk/cli, db, integrations-worker | Turn the org catalog into an internal developer portal: deep-linkable entity pages with a contextual sidebar, dependency graph, deployment/activity tabs, computed insights + scorecards, ownership/on-call, and a golden-path scaffolder — all while preserving the `18-state` invariant that the catalog read-model is git-derived, never console-authored. Evolves OP's OV7 + the U-track. |
| [`saas-catalog-portal/`](./saas-catalog-portal/) | **CP** | In progress | web-console-next, contracts/sdk, state-worker | Ship the org catalog as a world-class internal developer portal **matching the `Service Catalog` design pixel-for-pixel**: dark IDP surface with metric tiles, Table/Board/dependency-Map views, live filter/group, and a deep entity drawer (readiness scorecards, ops stats, ownership/on-call, dependency neighborhood). The design-faithful UI realization of `saas-service-catalog`; pairs `orun/specs/orun-catalog-portal` (**CPF**) for the git-authored fields. |
| [`saas-workspace-overview/`](./saas-workspace-overview/) | **WO** (presentation + catalog-model layer over **WS**/**CP**/**OP**) | ✅ Shipped (WO1–WO5; WO6 deferred) | state-worker, web-console-next, contracts/db; CLI half in `orun` | **The repo is the homepage.** The Workspace Overview landing (`/orgs/{slug}`): identity band + signal row + a git-authored narrative carried as a content-addressed blob in the catalog snapshot and rendered by digest — no provider coupling, no console authorship. Introduced the declared `Repo` kind, `docs.overview`, `state.repo_facet` + `doc_ref`. Post-ship review: `review-2026-07-05.md`; succeeded by `saas-catalog-docs`. |
| [`saas-catalog-docs/`](./saas-catalog-docs/) | **CD** (doc plane over **WO**/**CP**) | ✅ Shipped (CD0–CD6; CD7 deferred) | state-worker, web-console-next, contracts/sdk/db; CLI half in `orun`; `ogpic` reference adopter | **Every entity gets a shelf.** Generalizes WO's doc spine to a **doc set on every catalog kind** (`docs.pages` + enrichment for derived System/Domain), pinned-commit provenance, the org-wide `state.catalog_docs` index, and browsable console surfaces (Docs hub · reader · real entity Docs tab — replacing the synthesized files). No new object kind; derived, never authored. |
| [`saas-mcp-server/`](./saas-mcp-server/) | **MCP** (promotes the agent-surface half of P7) | Draft | packages/mcp (new), packages/cli, apps/mcp-worker (new), identity-worker, contracts/sdk, console, billing/metering workers | The AI-agent client surface: an MCP server exposing the catalog, runs/logs, audit, access, usage/billing, and config as ≤ 25 task-shaped tools. One tool registry (`packages/mcp`), two transports (stdio via the CLI; a remote Streamable-HTTP worker with `sk_` keys then OAuth 2.1 over OP1). **A client, not a service**: every tool call re-enters api-edge with the caller's credential, so RBAC/rate-limits/audit/metering apply unchanged. Read-only first; gated writes later; secrets metadata-only forever. |
| [`saas-agents/`](./saas-agents/) | **AG** (cloud control plane over the **orun** runtime; pairs **WP** + **MCP**) | Draft | new agents-worker, packages/db, identity-worker, web-console-next, metering/billing workers | **The cloud control plane for the agent runtime.** The runtime itself is the **orun binary** (`orun/specs/orun-agents/`, AG0–AG4): a local-first delegation loop where agent types are content-addressed objects (`agents/*.md` → `AgentTypeSnapshot`) and Claude Code (or any binary) sits behind a driver seam. This epic (AG5–AG11) hosts and governs it: a **Daytona** sandbox running `orun agent serve` under a session-scoped `sp_` principal, a per-session Durable Object relay to a new **Agents** sidebar tab, "Design with agent" on a Spec (epic files + `contract_propose` via `catalog affected`), and dispatch-is-assignment autonomous implementation runs the observation log tracks. Runtime is a client of public surfaces; no new identity plane, no work-plane status writes. The epic `orun/specs/orun-work/agents-and-mcp.md` §5 reserves; the runtime `saas-mcp-server` scoped out. |
| [`orun-work/`](./orun-work/) | **WP** (cross-repo) | Draft (v2 spec landed; v1 scrapped) | db, api-edge, integrations-worker, state-worker, web-console-next, contracts/sdk | The work lens: two append-only logs (coordination + observation) with **lifecycle as a derived query** — `Draft → Ready → In Progress → In Review → Done → Released`, each rung owned by a truth source, no stored status anywhere; pins are public attributed overrides rendered beside observed truth; agents get a write surface with no way to assert progress. Authoritative spec in `orun/specs/orun-work/` (v2); v1 (stored-status Initiative/Epic/Task + D1/DO sync) archived at `orun/specs/archive/orun-work-v1/` and torn down here (`490_work_teardown`). Pairs **P2** (`revision_live`), **IT** (webhook tenancy), **TO** (owner routing). Evolved by **PM** ([`orun-work-v3/`](./orun-work-v3/)). |
| [`orun-work-v3/`](./orun-work-v3/) | **PM** (over **WP**) | Draft (spec landed; PM0 not started) | web-console-next, state-worker, db, contracts/sdk; CLI/MCP legs in `orun` | The project surface: **Linear-class PM on the truth engine.** v2 proved lifecycle can't lie (fold over two logs, no stored status); v3 grows the intent plane a world-class cloud authoring surface — create/edit initiatives, specs (as content-addressed document revisions), and tasks; threaded comments + reactions + mentions; the unified timeline (both logs interleaved); a kanban board where dragging a card to "Done" **mints an attributed pin rendered beside observed truth** rather than writing a status; cycles with derived burn-ups; saved views; and the **project surface for agent teammates** (render/attribute/review/govern agent work — pairs **AG**, which owns the runtime + dispatch). Five nouns (adds Initiative, Cycle, View); the coordination vocabulary grows 9→19 kinds — **still no lifecycle-write kind**, the observation vocabulary frozen. Additive over v2 (no fold/schema/route breakage); repo import stays first-class. Pairs **AG** (`saas-agents`/`orun-agents`), **ES** (mentions), **U** (Northwind/Cmd-K), **TC** (@team). Milestones **PM0–PM5**. |
| [`saas-product-areas/`](./saas-product-areas/) | **P1, P3–P7** | Holding register | various | Promote-flow, observability, notification inbox, marketplace (P5 ⬆ promoted → [`saas-integrations/`](./saas-integrations/)), changelog/status, AI-native affordances. Each promoted to its own folder when work starts. |

The cross-epic sequencing and the one-line index live in
[`../roadmap.md`](../roadmap.md) — the program register.

## Lifecycle & conventions

- **Status legend:** see [`../README.md`](../README.md) § Status legend
  (`Draft → Ready → In progress → ✅ Shipped → ⛔ Blocked → Closed`).
- **As-built ≠ intent.** What actually shipped lives in each epic's
  `IMPLEMENTATION-STATUS.md`, kept distinct from the design/plan docs.
- **Milestone ✅, not archive.** A completed milestone inside an active epic is
  marked ✅ in `implementation-plan.md` and recorded in `IMPLEMENTATION-STATUS.md`
  — it is **not** deleted or archived. Only a **fully-closed program** (no open
  milestones, no follow-ups) moves to `../_archive/`.
- **Holding → promote.** A parked epic (`saas-product-areas` register) lives as a
  single README; when a leg is picked up it is promoted to a full doc set
  (`README.md` + `design.md` + `implementation-plan.md` + …), mirroring Orun's
  `orun-env-scoping` / `orun-affected-worker` pattern.
- **Sub-epics.** A tightly-coupled child program lives under its parent in
  `saas-<slug>/sub-epics/<child-slug>/` with the same doc set, and is surfaced
  from the parent README's milestone table (it is not a top-level register row).
  Today: `saas-multi-org-billing/sub-epics/billing-provider-abstraction/`.
- **Doc set per epic:** `README.md` (status table + thesis + read order +
  milestone-at-a-glance), `implementation-plan.md` (milestones with "done when"),
  `IMPLEMENTATION-STATUS.md` (as-built), plus `design.md` /
  `risks-and-open-questions.md` / `test-plan.md` where they carry weight.
