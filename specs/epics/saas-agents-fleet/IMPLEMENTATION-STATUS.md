# saas-agents-fleet — Implementation Status (as-built)

The as-built record for the cloud half (AF4–AF9), kept distinct from the
design/plan docs. The runtime half (AF0–AF3) is tracked in
`orun/specs/orun-agents-fleet/`.

| Milestone | Status | As-built |
|-----------|--------|----------|
| AF4 — The delegation plane | 🏗️ Core shipped | **The tree + the spawn door + tree kill + the orphan sweep.** Migration **`750_agents_fleet_tree`**: `agent_sessions` gains public-id-keyed `parent_session_id`/`root_session_id`/`depth` (backfilled roots, partial parent/root indexes); the session-event kind CHECK regenerates as one named constraint (the 720 discipline) adding `child_spawned`/`child_completed`/`child_failed` — the parent's sealed story of its children, still no status kind. `@saas/db/agents`: tree fields on the row + `CreateSessionInput` (`parentSessionId` + create-time `sandbox`), parent resolution in-transaction (root/depth inheritance), cross-org `listOrphanedSessions`. `@saas/contracts/agents`: `CapabilityCeiling` + **`intersectCeiling`** (the one rule: parent ∩ child, absent = unrestricted, associative — locked by an exhaustive 1296-case subset-property test), `TREE_LIMITS` (depth 2 · 5 children/parent · 10 live/tree), `DELEGATION_ERROR_CODES`, tree fields on the wire. `apps/agents-worker`: the **spawn door** in the create handler — an agent-session bearer takes the `organization.agent.session.spawn` grant (deny-by-default, distinct from create) and the gate stack (parent-live, depth, per-parent + per-tree width, ceiling intersection persisted as `sandbox.appliedCeiling`); the parent is ALWAYS the caller's own session (a body-supplied parent is unrepresentable). `POST …/sessions/{id}/cancel` — tree-transitive kill, leaf-up, interact-gated, best-effort sandbox destroys (shared `destroySandbox` extracted from the sweep); the `*/5` sweep gains the **orphan pass** (live child of a terminal parent past 5-min grace → `failed(orphaned)`, suspended → `canceled`). api-edge facade + SDK `cancelSession`. Console: the **children strip** + `Kill tree` on the session page, tree-gutter nesting on the fleet home (`orderFleetRows`), the conversation fold renders `child_*` narration (never flips terminal). Tests: 10 tree (door gates/leaf-up/route kill/orphan grace), 5 ceiling-property (contracts), 3 db tree, fold + ordering (console), facade + SDK. **Remaining AF4:** judge **verdict cards** as a distinct presentation (child_completed renders as a note line today), width/depth caps as workspace config (F-Q2), the budget-envelope gate (stub until AF8), live-runtime verification with orun AF0's `agent_spawn` fixtures. |
| AF5 — The attention plane | 🏗️ Core shipped | **The needs-you fold + the fleet home queue.** `@saas/contracts/agents`: the closed `ATTENTION_KINDS` vocabulary (enum-complete from day one — `budget`/`routine_parked` fold to zero until AF8/AF6), `ATTENTION_RANK`, `AttentionItem` (provenance-carrying) + `AttentionSummary`. `apps/agents-worker/src/handlers/attention.ts`: `foldAttention` — a pure computed read over session rows + relayed events (verdict from the latest unresolved `approval_requested`; `stuck` from a lapsed lease not yet swept; `failed_retryable` from task-bound failures inside a 24h window), ranked by kind then oldest fact; `GET …/agents/attention` read-gated (`agent.session.read`), routed + forwarded by the api-edge facade; SDK `agents.attention()`. Console: the fleet home (`agents-workbench.tsx`) rebuilt to the Northwind Agents mock order — stat pair (`running` / `need a verdict`, warn ink when nonzero, both from the same fold), the **Needs you queue** (`attention-queue.tsx`: verdict cards answer Approve/Deny in place by posting the same attach-v1 `verdict` frame the session page posts — the fleet home is a head; no dismiss affordance exists), Active/Recent session split with compact age column + honest failure lines, profiles with the truth caption, providers last; presentation model (`lib/agents/attention.ts`) pure + tested. Tests: 10 attention (fold + route + gates), facade route match, SDK URL shape, 5 presentation-model. **Remaining AF5:** the org-topbar/sidebar attention badge (needs a shell-level live query — the nav model is deliberately pure), the daily digest via notifications-worker, quick-spawn card (rides AG8's spawn surfaces). |
| AF6 — Routines | 🏗️ Core shipped | **Standing work, one door, quiet by default.** Migration **`760_agents_routines`**: `agents.routines` (trigger + binding rows — profile, run_kind, optional sealed `definition_ref`, `cron\|event` trigger_config, budget-stub caps, enabled, the park latch + `consecutive_failures`, `last_fired_at`; live partial index) and `agent_sessions.routine_id` for fleet grouping + park math. `@saas/db/agents`: `Routine` model + full repo surface (create/get/list/updateRoutineState/delete, cross-org `listLiveRoutines`, `listRoutineSessions`) in memory + Postgres. `@saas/contracts/agents`: `AgentRoutine` wire shapes, `ROUTINE_PARK_THRESHOLD` (2), error codes; `AttentionItem` gains `routineId`. `apps/agents-worker`: **`cron.ts`** — a minimal pure 5-field matcher (`*`, steps, lists, ranges, standard dom/dow either-match) with the **hourly floor** (`isHourlyOrCoarser`: the minute field must pin exactly one value — `* * * * *` is unrepresentable as a routine) and `dueSince` window math; registry routes (`GET/POST /agents/routines`, `PATCH/DELETE /agents/routines/{id}`, `agent.routine.read\|write`-gated) where **PATCH is standing-state only and parking is refused — only resume (which resets the latch + counter) is human-writable**; the dispatch door gains the **routine provenance** (`dispatchRoutineFiring`: enabled+unparked is the authorization — the ladder gates work dispatch, the standing order gates firings; one-live-run-per-routine dedupe + the shared concurrency cap still bite; the firing mark moves on attempt); **`tick.ts`** — the scheduler pass on the existing `*/5` cron: park-latch reconcile FIRST (two most-recent firings failed → parked with the failure named, never a third run on the parking tick), then cron due-check with **misfire-once semantics** (60-min lookback window off `last_fired_at` — predicates, not backlogs), firing through the same gate function below HTTP authorize (the sweep posture). Attention fold gains the `routine_parked` source (resume clears it — no dismiss). api-edge facade forwards the routine routes (+ PATCH joins the method allowlist, body forwarded); SDK `listRoutines/createRoutine/updateRoutine/deleteRoutine`. Console: the **Routines registry card** on the fleet home (On/Off/Parked pills, trigger + last-fired line, enable/disable/resume) + parked-routine attention cards with in-place Resume. Tests: 14 routines (matcher/floor/windows, CRUD + resume-only latch, firing + dedupe + not-live refusal, tick due/misfire/park/attention), 3 db repo, facade + SDK shapes — agents-worker 121, db 895+, sdk 208, api-edge facade 16, console 630 green. **Remaining AF6:** event-trigger firing (stored + inert until the ES1 lane consumer lands; re-check-against-the-fold rides it), the digest line for quiet outcomes (rides AF5's notifications remainder), a console create-routine form (registry is API/SDK-created today), `task_comment` on park for task-bound routines (needs the work-plane write surface). |
| AF7 — Track record & earned autonomy | 🗓️ Not started | — |
| AF8 — Budgets | 🗓️ Not started | — |
| AF9 — Hardening + evals | 🗓️ Not started | — |
