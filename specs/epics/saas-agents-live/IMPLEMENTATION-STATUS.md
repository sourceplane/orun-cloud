# saas-agents-live — Implementation Status (as-built)

The as-built record for the cloud half (AL6–AL9). The runtime half (AL0–AL5)
is complete in `sourceplane/orun` (`orun/specs/orun-agents-live/`).

| Milestone | Status | As-built |
|-----------|--------|----------|
| AL6 — The relay as attach server | 🏗️ Core shipped | **The cross-repo contract + the DO.** `packages/contracts/src/agents-attach.ts`: the TypeScript twin of the Go attach codec — the flat `AttachFrame` tagged union (keys mirroring the Go struct), typed constructors, NDJSON + SSE encode/decode, `isHeadInputFrame`/`isSealedEventFrame`. `packages/contracts/src/agents-attach-fixtures/`: the **six golden fixtures vendored verbatim from orun** (`internal/agent/attach/testdata`), round-tripped **byte-identically** by the TS codec (`tests/contracts/agents-attach.test.ts`, 11 tests) plus exact wire-form parity spot-checks — the cross-repo conformance that keeps the two codecs honest (attach-protocol.md §7). `apps/agents-worker/src/relay-core.ts`: `RelayCore` — the pure, storage-injectable relay logic (attach = hello → replay-from-cursor → live → fan-out; dedupe events by seq; durable mirror + cold-start `load()`; the head-input return-queue with **edge-stamped principal** and body-resolved acks; presence on attach/detach; terminal bye). `apps/agents-worker/src/relay-do.ts`: `SessionRelay` — the per-session Durable Object shell (one DO per session, the RunCoordinator per-run pattern) exposing the body-facing (`/events`/`/stream`/`/inputs`/`/inputs/ack`) and head-facing (`/attach` SSE, `/input`) surfaces over RelayCore, with SSE via a `TransformStream` and a bounded input-POST wait. Wiring: `SESSION_RELAY` DO binding in `env.ts` + `wrangler.template.jsonc` (top-level + per-env, SQLite migration), the DO exported from `index.ts`; the new `organization.agent.session.interact` policy action (distinct from read — driving a session ≠ watching it); head-facing `/attach` (read-gated) + `/input` (interact-gated, principal stamped from the resolved actor) routes in the router (`handlers/relay.ts`). Tests: 10 RelayCore (attach/replay/dedupe/fan-out/presence/return-queue/rehydrate/terminal), 11 contract-conformance; agents-worker suite 95 green, contracts typecheck + lint green, worker typecheck + lint green. **Remaining AL6:** the existing `/events` ingest also forwarding to the DO + R2 mirror of sealed frames; the body-facing `/relay/*` dial-out routes behind the three-way session gate; the api-edge facade forwarding attach/input (SSE pass-through). |
| AL7 — The console head | 🏗️ Core shipped | **The session page becomes a head.** `apps/web-console-next/src/lib/agents/conversation.ts`: `foldConversation` — the **shared presentation contract** with the orun TUI head (design decision 5): it folds the relayed session-event stream into structured conversation items (attributed user/agent turns, `⚙` tool cards with policy decision, note lines, sticky pending-approval cards, activity line from cost samples, terminal detection) exactly as `internal/tui/views/agent.go` does, so the two surfaces render identically. `components/agents/conversation-view.tsx`: renders the fold — turns, tool cards, and **sticky approval cards** (Approve/Deny, impossible to scroll off) above the composer. `session-detail.tsx` rebuilt from raw event rows into the head: the folded **Conversation** view, an always-on **composer** (Enter steers, attributed + sealed), and inline approval answering — all over `client.agents.sendInput` (the new SDK method) → the api-edge facade → the agents-worker `/input` route → the DO return-queue. **api-edge facade** (`agents-facade.ts`): the head-facing `attach` (SSE) + `input` routes recognized and forwarded with the edge-stamped actor (the principal the DO/body attribute inputs to). **SDK** (`packages/sdk/agents.ts`): `sendInput(orgId, sessionId, frame)` posting an attach-v1 head frame, returning the ack. Tests: 5 conversation-fold (turns/tools/sticky approval/resolution-attribution/terminal/activity/forward-compat), api-edge facade recognizes the new routes (13), console suite 13, sdk/api-edge/console/contracts typecheck + lint green. **Remaining AL7:** the SSE live tail replacing the 5s poll (the console consumes `GET …/attach` — rides AL8's api-edge SSE pass-through verification); tool-card expand, the inspector proof pane, the checklist affordance. |
| AL8 — Interchangeable heads + handoff | 🏗️ Core shipped | **orun is the supervisor; the terminal is a peer head.** `bootstrapScript` **retired** (locked decision 6): `provision.ts`'s in-sandbox exec now installs orun (if the image lacks it) and hands off to **`exec orun agent serve`** — the real runtime entrypoint (orun AL4). serve owns the heartbeat, token rotation, event ingest, and the relay return-queue; the control plane stops supervising the agent (the saas-agents §0 split, now real). The bash heartbeat/token loop is gone. Handoff: the console session page renders **Continue in terminal** — a copy-the-command affordance (`orun agent attach <id>`) so the same live session is driven from a terminal head, interchangeable with the console head (design §4). Principal attribution end-to-end and the api-edge attach/input facade already landed in AL7 (the edge stamps the authenticated actor; the DO/body attribute inputs to it), so the interchangeability property — console head and remote TUI head over one relay — is in place; the shared conversation-fold contract (AL7) guarantees identical rendering. Tests: the provision-flow test updated to assert the `orun agent serve` handoff (agents-worker suite 85 green); console typecheck + lint green. **Remaining AL8:** notifications (unattended-approval + terminal-state pushes via notifications-worker), presence chips in the console, the reverse console-deep-link surfaced to the TUI. |
| AL9 — Suspend/resume + metering | 🏗️ Metering shipped | **AG10's cost meters close.** `handlers/runtime.ts` `emitMetering`: the session-event ingest now derives the runtime usage meters from each relayed batch — **`agents.tokens`** summed over `cost_sample` events (the samples the Claude Code driver finally emits, orun AL1), and **`agents.session_minutes`** on the terminal `state_changed`, from the lease bracket startedAt→now (≥1, ceil). Both fire-and-forget through the shipped `deps.usage` recorder with `{runKind}` dimensions — a lost sample is a reconciliation problem, never a failed ingest; a non-terminal state change meters nothing. This closes the AG10 remainder the design flagged (the meters that needed cost samples flowing). Tests: 1 new (tokens sum across two ingests, minutes once on terminal, nothing on a non-terminal change, dimension carried) — agents-worker suite 86 green; typecheck + lint clean. **Remaining AL9:** the suspend/resume choreography (re-provision from snapshot → `orun agent serve --resume` continuing the harness session captured as `harness_event`, heads re-attaching on cursor) and R2/index retention GC — both need live Daytona to exercise end-to-end, so they ride the credentialed integration slice with AL6's DO relay live verification. |
