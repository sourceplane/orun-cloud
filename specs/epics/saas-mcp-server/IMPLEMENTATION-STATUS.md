# saas-mcp-server — Implementation Status (as-built)

What actually shipped, kept distinct from the design/plan docs per
`../README.md` § Lifecycle & conventions.

| ID | Milestone | Status | As-built |
|----|-----------|--------|----------|
| MCP0 | Tool-plane foundation (`packages/mcp`) | ✅ Shipped | `@saas/mcp` turbo-package: transport-agnostic registry (`defineTool` in `tool.ts`, public surface `registry.ts` — split to avoid an ESM cycle), `createMcpServer({ sdk, readOnly?, limits? })` over `@modelcontextprotocol/sdk` ^1.29 (spec rev 2025-06-18, risk D6 marker in `server.ts`), SDK-error→tool-error mapper preserving the platform `code` set, workspace/project scope fragments, UTF-8-safe 64 KiB truncation. **19 read-only tools** (≤ 25 budget): whoami, workspaces_list, projects_list, catalog_search, catalog_get_entity (emulates via OV6 list — D2, SC0 still unshipped), catalog_read_doc (CD), runs_list/runs_get/runs_read_logs (OV7), audit_search, events_search (ES), security_events_list, access_explain, usage_summary, quota_check, billing_summary, config_read, secrets_list (metadata-only with a fail-loud value guard + test), webhook_deliveries_list. 65 vitest tests incl. an `InMemoryTransport` end-to-end. Component intent: `dependsOn` build-input edges on `contracts` + `sdk`. |
| MCP1 | Local stdio server via CLI | ✅ Shipped | `orun-cloud mcp` command group in `packages/cli` (`commands/mcp.ts`, registered in `cli-runner.ts`): `mcp serve` connects a `StdioServerTransport` to `createMcpServer` with the stored token (keychain/file — same path as every command; absent → exit 3 + `orun-cloud login` pointer on stderr, no server started) and a base URL of `--api-url` > context `lastApiUrl` > credential `apiUrl`; stdout carries only the protocol (banner/diagnostics on stderr, asserted in tests + smoke). `mcp tools` prints the roster (human table / `--output=json`), honouring `--read-only` (no-op today — all 19 tools are read-only — but plumbed through). Ambient workspace defaulting per design §3: new `defaultWorkspace?` on `CreateMcpServerOptions` + exported `applyWorkspaceDefault` in `packages/mcp` fills `workspace` only when the tool's schema has the field and the caller omitted it (advertised schema makes `workspace` optional on the wire when a default is active); CLI precedence `--workspace` flag > active org from `context/store.ts` > none. Deps: `@saas/mcp` + `@modelcontextprotocol/sdk` added to `@saas/cli`; `mcp` build-input edge in `component.yaml`. Docs: "MCP server" section in `packages/cli/README.md` with Claude Code/Cursor/VS Code/generic snippets. Tests: 8 mcp-defaulting tests (`workspace-default.test.ts`) + 10 CLI tests (`mcp-commands.test.ts`); stage E2E ("who owns X?" via Claude Code) not exercised — no seeded stage org in the dev loop; local stdio JSON-RPC smoke (initialize → tools/list, 19 tools) verified instead. Remote transport/OAuth remain MCP2/MCP3. |
| MCP2 | Remote `apps/mcp-worker` | ✅ Shipped | New `cloudflare-worker-turbo` component, vars-only wrangler template (per-env `API_EDGE_URL`; **no service bindings/DB/KV/DO** — the worker's only egress is the public api-edge URL via `@saas/sdk`, client-not-service). Transport: the MCP TS SDK's `WebStandardStreamableHTTPServerTransport` (fetch-native, in ^1.29) in **stateless mode** (risk D5): per `POST /mcp` (also `POST /`) a fresh `createMcpServer({ sdk, readOnly: true })` + transport with `enableJsonResponse: true` — single `application/json` responses (spec-permitted in place of SSE), no `Mcp-Session-Id` ever issued, client-sent session ids accepted-and-ignored; `GET`/`DELETE /mcp` → 405 `Allow: POST` (spec-permitted for stateless servers); JSON-RPC parse errors → -32700. Auth: bearer forwarded verbatim (opaque here — `sk_` keys and AG6 session tokens identical by construction); missing/malformed → 401 + `WWW-Authenticate: Bearer resource_metadata="…/.well-known/oauth-protected-resource"` (that route 404s until MCP3; the `mcp.<domain>` custom hostname is likewise an MCP3/infra follow-up — workers.dev for now). `readOnly: true` is deliberate: keeps the MCP5 write set off the remote surface until enabled (design §7). Self-protection (design §8): per-isolate in-flight cap (32 → 429 + `Retry-After`, in-memory, fail-open); `GET /health` with `{ ok, name, version, toolCount }`; structured request logs (method/path/status/duration/requestId — never the bearer). Tests: 12 in-app vitest (initialize round-trip, 19-tool list, tools/call over a stubbed api-edge fetch asserting verbatim bearer forwarding, 401/405/-32700/404-metadata/503-unwired/health, cap saturation + recovery) + `tests/mcp-worker` quick-check smoke (6 jest tests, mirroring `tests/agents-worker`). **Live-stage acceptance not exercised in this dev loop** (no MCP client pointed at stage, no seeded viewer-role key for the RBAC-forbidden check) — RBAC enforcement is by construction (every call re-enters api-edge) but the stage smoke remains for the deploy lane/MCP8. |
| MCP3 | OAuth 2.1 | ✅ Shipped | **D1 decided: Option A** — vetted public-client allow-list (`OAUTH_PUBLIC_CLIENTS` in `@saas/contracts/auth`: claude-code, claude-web, cursor, vscode, orun-cloud-dev [loopback-only]; exact redirect match + RFC 8252 §7.3 loopback any-port carve-out), NO open DCR. **R5 held: zero new token plane** — an MCP grant IS an OP1 cli-kind session labeled `mcp:<clientId>` via the existing `client_host` column; the authorization code rides `identity.cli_login_grants` as a third `flow='oauth'` (migration 670: code hashed into the existing `cli_code_hash` + `oauth_client_id`/`oauth_redirect_uri`/`oauth_code_challenge` binding columns; same single-use pending→approved→redeemed semantics). identity-worker: `GET /.well-known/oauth-authorization-server` (RFC 8414, raw JSON; issuer = `OAUTH_REDIRECT_BASE_URL`, authorization_endpoint = console `/oauth/authorize`, S256-only, token auth "none"); `POST /v1/auth/oauth2/authorize/complete` (console-internal, actor-authenticated; ~60s single-use code after consent; `oauth.grant.created` security event); `POST /v1/auth/oauth2/token` (RFC 6749: form-encoded in, RAW OAuth JSON out with `Cache-Control: no-store`, §5.2 error codes; authorization_code verifies client/redirect binding + PKCE S256 then mints via OP1 `mintSession`; **code replay ⇒ `invalid_grant` + revocation of the minted refresh family** + `oauth.code.replay_detected` event; refresh_token = the unchanged OP1 rotation incl. reuse-detection/grace). api-edge auth facade: the two public routes + authenticated authorize/complete (rate-limit family `auth`). mcp-worker: `/.well-known/oauth-protected-resource` (RFC 9728) live off the new per-env `OAUTH_AUTHORIZATION_SERVER_URL` var (404 when unset; worker stays binding-free). Console: minimal `/oauth/authorize` consent page (existing login shell; allow-list-validated params; Approve → authorize/complete → redirect with code+state; Deny → `error=access_denied`; never redirects to an unregistered URI) + the existing Sessions & devices list renders MCP grants via the `mcp:` label (revoke path unchanged, test-verified). SDK: `auth.oauthAuthorizeComplete`. Deliberate descopes: no org selector on consent — tokens are workspace-agnostic per OP1 semantics (same as CLI logins); grants visibility = client label in the existing sessions list (no new page); refresh requests are not client_id-bound (the rotating single-use refresh + family revoke is the defense). Tests: 22 identity-worker (metadata shape; allow-list incl. loopback port wildcard; S256-required; code single-use/TTL; PKCE good/bad; replay→family revoke; refresh rotation; console revoke), 6 api-edge facade, 5 mcp-worker (metadata shape/unset-404/405 + quick-check smoke), 9 contracts (allow-list + RFC 8252 matching), 12 console (consent parsing/redirect building/session labels), 1 SDK. Live-stage E2E with a real MCP client (plan "done when") remains a deploy-lane follow-up, as with MCP1/MCP2. |
| MCP4 | Resources & prompts | ✅ Shipped | All in `packages/mcp` — both transports inherit via `createMcpServer` (no CLI/worker changes). **Resources** (`resources.ts`, design §6, exactly 2 templates, MIME `text/markdown`): `catalog://{workspace}/{entityKey}` — entity overview via the same OV6 exact-filter emulation as `catalog_get_entity` (D2) + `listCatalogDocs(entityRef)` + the overview doc body (byte-capped with the standard truncation marker), rendered as identity/relations/provenance/doc-index/overview markdown; `entityKey` = **base64url(entityRef)** (refs contain `:`/`/` which can't ride one URI path segment; codec exported as `encodeEntityKey`/`decodeEntityKey`). `runs://{workspace}/{project}/{runId}` — run header + git provenance + per-job status table (`getRun` + `listRunJobs`, same as `runs_get`). **Both templates' `list` callback returns an empty list on purpose**: enumerating a whole org's catalog/run history on `resources/list` would blow the client's context budget for no navigational value — agents discover ids via `catalog_search`/`runs_list`, then attach the URI. Resource reads have no `isError` channel, so a new `ResourceReadError`/`toResourceReadError` in `errors.ts` rethrows with the tool-error framing (`<code>: <detail> (requestId: …)` — forbidden/not_found/validation_failed surface in the protocol error message). **Prompts** (`prompts.ts`, 4): `investigate_failed_run` (orient via `runs_list status=failed` when no runId → `runs_get` → `runs_read_logs` with `fromSeq` paging → root-cause report), `access_review` (`access_explain` + `security_events_list` + `audit_search` → provenance table), `usage_review` (`usage_summary` + `quota_check` + `billing_summary` → ≥ 80% flags), `service_snapshot` (`catalog_get_entity` + `catalog_read_doc` + `runs_list` on the entity's sourceProjectId → service brief). Prompt text is deliberately terse and names tools by exact registered name; a **drift-guard test** extracts every snake_case token from rendered prompt text (all arg branches) and asserts it resolves in `allTools` — a tool rename fails the suite instead of orphaning a prompt. `readOnly` never filters resources/prompts (read-only/text by construction; asserted in tests). Tests: +24 (13 resources, 11 prompts; protocol-level over `InMemoryTransport`), suite now 97. Tool budget untouched (still 19 tools). |
| MCP5 | Write tools (gated) | ✅ Shipped | All in `packages/mcp`. **The design §4 write set, exactly 6 tools**: `project_create`/`environment_create` (`tools/projects.ts` → `repos.create`/`environments.create`), `flag_set` (`tools/config.ts` — set-shaped upsert over the SDK's create/update flag pair: scope-exact `listFeatureFlags` lookup by `flagKey`, then update-in-place or create; requires ≥1 of `enabled`/`value`), `webhook_create` (`tools/webhooks.ts` — org- or project-scoped endpoint create + optional `events[]` fan-out to subscription creates on DERIVED idempotency keys `<base>:sub<i>`, deterministic so a retried call replays every leg; capped ≤255), `webhook_delivery_replay` (`replayDelivery`; input is `workspace`+`delivery` attempt id — the SDK op doesn't need the endpoint id), `member_invite` (`memberships.createInvitation`; the response's one-time accept token (`delivery.token`) is deliberately STRIPPED — credential-shaped material never reaches an agent; test-pinned). **Tool budget now exactly consumed: 19 reads + 6 writes = 25/25 — a future tool must displace one.** Excluded writes per design §4 hold: no api-key/billing/team-grant/admin mutations, no work-plane tools (locked decision 8), no secret writes. **Idempotency (§7)**: new `idempotency.ts` — every write resolves a key per logical attempt (`mcp_<uuid>` auto; optional `idempotencyKey` input validated by the contracts `parseIdempotencyKey`, 1–255 printable ASCII) and sends it via the SDK's existing per-request `RequestOptions.idempotencyKey` seam (no SDK changes). **Provenance (§7)**: `createMcpServer` stamps `x-client-surface: mcp` into the SDK transport's `defaultHeaders` at construction, so ALL calls (reads, writes, resource reads) on both transports carry it for free (`CLIENT_SURFACE_HEADER`/`CLIENT_SURFACE_VALUE` exported; protocol-level tests assert it on a read and a write with a fetch spy; mcp-worker test asserts it alongside verbatim bearer forwarding). **Annotations**: writes are `readOnlyHint:false, destructiveHint:false, idempotentHint:true`; all 19 reads retroactively gained explicit `destructiveHint:false`. **Read-only mode**: `readOnly: true` now actually filters — new exported `readOnlyTools` roster; CLI `mcp serve`/`mcp tools` without `--read-only` exposes 25 (write rows marked "no" in the read-only column), with it 19; `apps/mcp-worker` stays `readOnly: true` (remote writes are a DELIBERATE later enable — comment pinned in `handlers/mcp.ts`; health `toolCount` now reports the served read-only 19). Descriptions state the write is policy-gated + audited; RBAC/audit ride api-edge unchanged (client-not-service). Tests: `packages/mcp` 97→126 (24 write-tool tests: happy path incl. key on the request options, supplied-key passthrough, forbidden, distinct-auto-keys/stable-supplied-key; registry pins 25/order/annotation matrix/readOnly exclusion; 2 provenance protocol tests), mcp-worker 15 (write-exclusion + provenance added), CLI 255 (roster/markers/--read-only updated). Stage acceptance (builder-key round-trip, viewer-forbidden, edge replay on duplicate key) remains for the deploy lane, as with MCP1–MCP3. |
| MCP6 | Metering + entitlement | ✅ Shipped | **D3 default posture: gate OPEN** (granted to all plans; the free-vs-paid product decision is still open — annotated in `risks-and-open-questions.md`). **Metering** (`packages/mcp/src/usage.ts`): every SUCCESSFUL tool call emits one `mcp.tool_call` usage event (quantity 1) through the STANDARD public ingest — `sdk.metering.recordUsage` → `POST /v1/organizations/{workspace}/usage` on the caller's own credential (client-not-service holds; agent metering rides the same pipe as customer metering, and carries `x-client-surface: mcp` for free). Tool + transport dimensions ride the ingest contract's existing bounded `metadata` field (`{ tool, transport: "stdio"|"http" }` — no contract change); `idempotencyKey` is `mcp_call_<uuid>` per logical call. FIRE-AND-FORGET: kicked off synchronously post-handler, never awaited on the tool path, failures swallowed with an optional debug note (CLI: stderr; a metering outage cannot break agent traffic); workspace attribution uses the call's `workspace` argument (ambient default included) — workspace-less tools (whoami/workspaces_list) emit nothing rather than guessing tenancy; failed tool calls emit nothing. Opt-in via new `CreateMcpServerOptions.usage { enabled, transport, schedule?, debug? }` — default OFF so unit tests and `mcp tools` stay silent; CLI `mcp serve` enables (stdio, detached promise), mcp-worker enables (http, `ctx.waitUntil` threaded through `route`). **Entitlement** (`feature.mcp_server`): declared enabled on all 4 `PLAN_CATALOG` tiers + the check-entitlement safety net (billing-worker; mirrors `feature.integrations.github`, the all-plans precedent). Gate lives at the TRANSPORT seam — the tool plane (`tools/`, `executeTool`) stays billing-blind; the plan's "nothing in packages/mcp knows about billing" holds at the tool plane, with the seam helper (`entitlement.ts`: `checkMcpServerEntitlement` over the PUBLIC `GET /billing/entitlements` read + `createEntitlementGate`) living in `packages/mcp` and wired ONLY by transports via the new `CreateMcpServerOptions.gate`. Semantics: explicit enabled row → granted; MISSING row → granted (open-gate default; pre-key orgs unaffected); explicit disabled row → the platform's upgrade-shaped error (`entitlement_required` + `entitlementKey` detail, the events-worker ES5 pattern) through the standard tool-error mapper (`EntitlementDeniedError` in `errors.ts`); failed read → fail-open (edge posture; revisit with D3). CLI checks ONCE at `mcp serve` startup against the ambient default workspace (denied → clear stderr + exit 6, no server; no workspace → skip); worker checks lazily on the first tool call carrying a workspace, cached per-isolate per-org (60s TTL, 256-entry cap, cache in `deps.ts` so tests inject fresh Maps). **Descopes:** optional `mcp.tool_call` quota not configured (existing quota machinery can add it later, zero MCP changes); no console UX beyond what exists (`usage_summary` dogfood asserted in tests — the ingested metric round-trips the summary read shape). Tests: `packages/mcp` 126→153 (+15 usage incl. fire-and-forget/never-blocks/uniqueness/protocol wiring, +12 entitlement incl. cache TTL/denial mapping/flip-without-redeploy), mcp-worker 15→21 (+6: cache-across-requests, missing-row granted, denial surfaces `entitlement_required`, http-dimension event shape, ingest-failure isolation, whoami ungated/unmetered) + quick-check smoke 7, CLI 255→260 (+5 startup-gate paths), billing-worker component tests +2 (all-tiers grant + safety-net grant). |
| MCP7 | Console Connect surface | ✅ Shipped | **D4 decided: role-based tool scoping only in v1** (the key's role is the tool boundary; the `PublicApiKey.toolScopes` extension is the recorded later path — see risks doc). Console-only + docs — **no new backend endpoints or read models**. New workspace-scoped page `Settings › Developer › MCP server` (`/orgs/[orgSlug]/settings/mcp`, `settings-nav.ts` Developer group next to API keys per the SI IA; Cmd-K `nav.mcp` entry): (1) **Local (recommended)** — tabbed per-client snippets with `CopyButton` (Claude Code `claude mcp add orun-cloud -- orun-cloud mcp serve`, Cursor `~/.cursor/mcp.json`, VS Code `.vscode/mcp.json`, generic stdio), mirroring `packages/cli/README.md` verbatim, with the `orun-cloud login` prerequisite + `--read-only`/`--workspace` flag notes; (2) **Remote** — per-env Streamable-HTTP URL derived like every other worker URL (`mcpWorkersDevUrl` beside `apiEdgeWorkersDevUrl` in `app-config.ts`, env from the session's API target; `mcp.<domain>` hostname still the tracked infra follow-up), `sk_` bearer with a "Create API key" link to the EXISTING api-keys page (viewer for read-only agents, builder for writers — minting not rebuilt) and OAuth for interactive clients listing the vetted `OAUTH_PUBLIC_CLIENTS` names (dev client filtered); (3) **Access & governance** — role bounds tools (reads=viewer, writes=builder+, People & Access link), remote read-only today, every call policy-checked/rate-limited/audited (`x-client-surface: mcp`)/metered (`mcp.tool_call`), secrets metadata-only; (4) **Status** — "Your connected agents" reuses the per-user `cliSessions.list()` read (MCP3: a grant IS an `mcp:`-labeled session; `activeMcpGrants` filter), revoke stays on Sessions & devices, usage links to the existing Usage explorer (no metric deep-link — the page has no query-param filter). Page model is a pure module (`components/settings/connect-agent.ts`) per console test conventions. Docs parity: `apps/web-docs/docs/developers/mcp.md` + sidebar entry under Developers. Tests (`tests/web-console-next`): +13 `connect-agent.test.ts` (snippets verbatim incl. the Claude Code string, JSON snippets parse, remote URL, no secret material, allow-list roster, links, grant filtering), +1 settings-nav (Developer placement next to API keys), +1 command-registry (`nav.mcp`), +1 breadcrumbs (`mcp` segment label) — component suite 519→535. "Under two minutes without leaving the page" not user-tested; live grant/usage state = the sessions-read grants panel + usage link. |
| MCP8 | Conformance + agent evals | ✅ Shipped | New `tests/mcp` verifier component (`mcp-tests`, `@saas/mcp-tests`; jest, quick-check dev lane — mirrors `tests/mcp-worker`, which keeps its narrower worker smoke; no name collision: `mcp-tests` vs `mcp-worker-tests`). **Tool-budget guard** (R1, `src/budget.test.ts`): `allTools` pinned EXACTLY 25 (a 26th fails CI), `readOnlyTools` exactly 19, unique snake_case names, every description non-empty ≤ 500 chars, every tool's readOnly/destructive/idempotent hints all present — plus a context-cost ceiling on the aggregate WIRE-serialized input schemas of the default `tools/list` (measured 2026-07-09: **21,302 bytes** at 25/25; ceiling pinned **32,000 ≈ 1.5×** with the measurement recorded in-file). **Contract pins** (R4, `src/contracts.test.ts`): runtime complement to the compile-time `satisfies` discipline — fixture DTOs typed AS the contracts types (`src/fixtures.ts`: OrgCatalogEntity, CatalogDoc, Run+RunJob+ReadLogResponse, PublicEvent, PublicAuditEntry, PublicSetting/PublicFeatureFlag/PublicSecretMetadata, usage/quota responses, AuthUser/PublicOrganization; `@saas/testing` checked first — its fixtures cover none of these, so they live locally) flow through each tool's happy path and the structured output must preserve the load-bearing fields (a contract rename is a compile error in the fixture, a silently-dropping tool a runtime failure); secrets output additionally asserted value-material-free. **Protocol conformance, both transports**: local matrix (`src/conformance-local.test.ts`) drives `createMcpServer` — the exact object `mcp serve` connects stdio to — over `InMemoryTransport` via a raw JSON-RPC driver (`src/raw-client.ts`, deliberately not the SDK `Client` so wire facts are assertable): initialize pins protocolVersion **2025-06-18** (D6) + tools/resources/prompts capabilities + serverInfo, tools/list 25-in-registry-order / 19 readOnly, tools/call happy path, unknown tool + invalid args → isError results (-32602 text) with connection liveness after, unknown method → -32601, resources/templates/list = 2, prompts/list = 4 + prompts/get renders. *Stated choice per plan:* process-level stdio spawning of the built CLI was skipped — the repo convention is in-process transports everywhere and no component builds sibling dists; stdio framing stays with the CLI's MCP1 tests/smoke. Remote matrix (`src/conformance-remote.test.ts`) drives `apps/mcp-worker`'s `route()` with an injected seeded api-edge fetch stub (the `tests/mcp-worker` pattern) through the same matrix plus transport cases: 401 challenge with RFC 9728 metadata pointer, malformed bearer 401, verbatim bearer forwarding + `x-client-surface: mcp` on every SDK call, -32700 on unparseable JSON, statelessness (no session id). **Agent-task evals (on-demand, NOT per-commit)**: `evals/scenarios.ts` + `evals/harness.eval.ts`, run via `pnpm --filter @saas/mcp-tests evals` (separate jest `--testMatch`; excluded from the default test command and the verify lane). 4 scenarios — find-owner-of-billing-worker, diagnose-failed-run (runs_get BEFORE runs_read_logs, ordered), org-near-quota, what-changed-yesterday — executed as scripted tool-call traces through the real `executeTool` seam against the seeded-org stub SDK, recorded to `evals/traces/*.json` + `summary.json` (gitignored — the repo commits no test artifacts), scored on: required tool subsequence, forbidden tools (all 6 writes) absent, expected output markers present, no step errored. *Deliberate scope:* deterministic AFFORDANCE evals (can the curated toolset express the task; do outputs carry what the task needs); LLM-in-the-loop evals (do models CHOOSE these tools from the descriptions) are the follow-up once an eval runner exists. Tests: 39 verify-lane (6 budget, 13 contract pins, 11 local + 9 remote conformance) + 4 eval scenarios; component typecheck/lint/test green; `@saas/mcp` 153 and mcp-worker 21 + smoke 7 unchanged (no product code touched). |

## Notes for later milestones

- State-plane cursors surface as the `createdAt|id` string the endpoints
  accept (matches the console's encoding); opaque cursors (audit, events,
  security-events, webhooks) pass through verbatim; all paginated tools return
  `data.meta.cursor`.
- The SDK does not export its query option types; `packages/mcp` derives them
  via `Parameters<StateClient[...]>` so they stay pinned to real signatures —
  if the SDK exports them later, switch.
- `webhook_deliveries_list` with no `endpoint` argument lists the workspace's
  endpoints (delivery attempts are per-endpoint; no separate endpoints tool —
  budget).

## Unification phase (D7) — paired with `orun/specs/orun-mcp/`

| ID | Milestone | Status | As-built |
|----|-----------|--------|----------|
| MCP9 | Tool-manifest export (`packages/mcp/tool-manifest.json`) | 🗓️ Planned | — |
| MCP10 | Docs flip to `orun mcp serve` (console/web-docs/CLI README) | 🗓️ Planned | — |
