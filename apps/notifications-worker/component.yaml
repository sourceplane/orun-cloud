apiVersion: sourceplane.io/v1
kind: Component
metadata:
  name: notifications-worker
  description: Cloudflare Worker for the Notifications bounded context
spec:
  type: cloudflare-worker-turbo
  lifecycle: production
  owner: platform
  domain: starter-runtime
  path: apps/notifications-worker
  dependsOn:
    # Build inputs (dependsOn[].input, orun v2.22.0): these workspace packages
    # are bundled into this component's artifact, so a change to any of them
    # rescopes this component in --changed (no more stale-bundle deploy gaps).
    # Inert under the pinned orun v2.15.0; activates on the v2.22 CI cutover.
    - component: contracts
      input: true
    - component: db
      input: true
    - component: db-migrate
    - component: membership-worker
    - component: policy-worker
    - component: billing-worker
  subscribe:
    environments:
      - name: dev
        profile: verify
      - name: stage
        profile: verify
        profileRules:
          - profile: deploy
            when:
              triggerRef: github-push-main
      - name: prod
        profile: verify
        profileRules:
          - profile: deploy
            when:
              triggerRef: github-push-main
  parameters:
    nodeVersion: "22"
    pnpmVersion: "10.12.1"
    workspaceDir: ../..
    appDir: .
    # BF6b: deploy-time wiring — wrangler.jsonc is rendered, never committed.
    # Verify lanes render from wiring.fixture.json (offline); deploy lanes
    # resolve real IDs from the Secrets Manager wiring manifest (BF5).
    awsAccountId: "306024784101"
    awsRegion: us-east-1
    orgName: sourceplane
    owner: sourceplane
    repo: orun-cloud

    turboFilter: "@saas/notifications-worker"
    wranglerConfig: wrangler.jsonc
    wranglerTemplate: wrangler.template.jsonc
    wiringComponents: "cloudflare-hyperdrive"
    wiringEnvs: "stage,prod"
    wiringFixture: wiring.fixture.json
    installCommand: pnpm install --frozen-lockfile
    buildCommand: pnpm exec turbo run build --filter=@saas/notifications-worker
    typecheckCommand: pnpm exec turbo run typecheck --filter=@saas/notifications-worker
    dryRunCommand: pnpm exec wrangler deploy --dry-run --config wrangler.jsonc --env prod
    deployCommand: pnpm exec wrangler deploy --config wrangler.jsonc --env prod
    preDeployCommand: "echo 'No pre-deploy step configured.'"
    migrationCommand: "echo 'No migrations configured.'"
    smokeCommand: "echo 'Internal-only worker; no public smoke endpoint.'"
  labels:
    team: platform
    layer: runtime
    surface: notifications
    runtime: cloudflare
# ci: trigger full-component verification (orun v2.12.0 upgrade — PR #183)
# ci: rebuild marker — redeploy for PX3 preferences facade callers (stage/prod drift from the failed #280 convergence run)
# ci: redeploy with live Hyperdrive wiring (BF6b convergence)
# ci: bootstrap deploy without service bindings (binding-cycle seed)
# ci: restore service bindings after binding-cycle seed
