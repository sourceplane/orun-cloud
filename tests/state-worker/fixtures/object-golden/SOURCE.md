# Vendored: object-model golden vectors

`golden-vectors.json` is generated by **orun** and vendored here so this repo's
object-model reader (`apps/state-worker/src/object-model.ts` +
`catalog-projection.ts`) is pinned to the same wire format + field semantics in
CI. Do not hand-edit.

- Source: `github.com/sourceplane/orun` — `internal/objgolden`
- Pinned ref: `57550a4aed4769d868ae41cf2eef3be273f3c40b`
- Regenerate (in orun): `go test ./internal/objgolden/ -update`, then copy
  `internal/objgolden/testdata/golden-vectors.json` here and bump the ref above.

If `object-golden.test.ts` fails after an orun bump, the TS reader has drifted
from the Go wire format — fix the reader (or the projection mapping), don't edit
the fixture.
